# 🖥️ Выбор и настройка сервера на Reg.ru

## 📦 Рекомендуемые тарифы

> ⚠️ **Важно:** Не покупайте тарифы с 1 CPU и 1GB RAM - они не подойдут для SendBuddy!

### Для одного проекта (SendBuddy):

**Минимальный вариант (обязательно!):**
- **CPU**: 2 ядра
- **RAM**: 2GB
- **Диск**: 20GB SSD
- **Стоимость**: ~600-800₽/месяц
- **Подходит для**: тестирования, небольшой нагрузки
- ⚠️ **Меньше не покупайте!** (1 CPU/1GB не хватит)

**Рекомендуемый вариант:**
- **CPU**: 4 ядра
- **RAM**: 4GB
- **Диск**: 40GB SSD
- **Стоимость**: ~600-800₽/месяц
- **Подходит для**: продакшена, средняя нагрузка

### Для двух проектов:

**Минимальный вариант:**
- **CPU**: 4 ядра
- **RAM**: 4GB
- **Диск**: 40GB SSD
- **Стоимость**: ~600-800₽/месяц

**Рекомендуемый вариант:**
- **CPU**: 6 ядер
- **RAM**: 8GB
- **Диск**: 60GB SSD
- **Стоимость**: ~1200-1500₽/месяц

## 🛒 Как купить сервер на Reg.ru

### 1. Перейдите на сайт Reg.ru Cloud

1. Зайдите на [www.reg.ru/cloud](https://www.reg.ru/cloud/)
2. Войдите в аккаунт или зарегистрируйтесь
3. Перейдите в раздел "Облачные серверы"

### 2. Выберите конфигурацию

- Выберите регион (ближайший к вашим пользователям)
- Выберите операционную систему: **Ubuntu 22.04 LTS** (рекомендуется)
- Выберите конфигурацию из списка выше

### 3. Настройте сервер

- **Имя сервера**: sendbuddy-server (или любое другое)
- **Пароль root**: создайте надежный пароль (сохраните его!)
- Или загрузите SSH ключ (рекомендуется)

### 4. Оплатите

- Выберите период оплаты (месяц/год)
- Оплатите заказ

## 🔑 Что вы получите после покупки

1. **IP адрес сервера** (например: 185.123.45.67)
2. **Логин**: root (или указанный вами)
3. **Пароль**: тот, что вы указали при создании
4. **SSH доступ** для подключения

## 📋 Следующие шаги после покупки

### Шаг 1: Подключитесь к серверу

```bash
ssh root@ваш-ip-адрес
# Введите пароль при запросе
```

Или с SSH ключом:
```bash
ssh -i ~/.ssh/your_key root@ваш-ip-адрес
```

### Шаг 2: Обновите систему

```bash
apt update && apt upgrade -y
```

### Шаг 3: Установите Docker

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
```

### Шаг 4: Установите Docker Compose

```bash
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```

### Шаг 5: Проверьте установку

```bash
docker --version
docker compose version
```

### Шаг 6: Настройте firewall

```bash
apt install ufw -y
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw enable
```

### Шаг 7: Клонируйте проект

```bash
git clone https://github.com/ваш-username/SendBuddynew.git
cd SendBuddynew
```

### Шаг 8: Настройте переменные окружения

```bash
cp .env.production.example .env.production
nano .env.production
```

Заполните:
- `DB_PASSWORD` - сгенерируйте: `openssl rand -base64 24`
- `JWT_SECRET` - сгенерируйте: `openssl rand -hex 32`
- `FRONTEND_URL` - ваш домен или IP
- `VITE_API_URL` - ваш домен/api или IP:3001/api

### Шаг 9: Запустите деплой

```bash
chmod +x deploy.sh
./deploy.sh
```

## 🌐 Настройка домена (опционально)

Если у вас есть домен:

1. **В панели управления доменом** добавьте A-запись:
   - Имя: `@` (или `www`)
   - Значение: IP адрес вашего сервера
   - TTL: 3600

2. **Подождите** распространения DNS (5-30 минут)

3. **Настройте Nginx** (см. [DOCKER_COMPOSE_GUIDE.md](DOCKER_COMPOSE_GUIDE.md))

4. **Установите SSL**:
   ```bash
   apt install certbot python3-certbot-nginx -y
   certbot --nginx -d ваш-домен.ru
   ```

## 💡 Полезные советы

### Безопасность

1. **Используйте SSH ключи** вместо паролей:
   ```bash
   # На вашем компьютере
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ssh-copy-id root@ваш-ip-адрес
   ```

2. **Отключите вход по паролю** (после настройки SSH ключей):
   ```bash
   nano /etc/ssh/sshd_config
   # Установите: PasswordAuthentication no
   systemctl restart sshd
   ```

3. **Настройте fail2ban** для защиты от брутфорса:
   ```bash
   apt install fail2ban -y
   systemctl enable fail2ban
   systemctl start fail2ban
   ```

### Мониторинг

1. **Проверка использования ресурсов**:
   ```bash
   htop
   # или
   free -h
   df -h
   ```

2. **Проверка статуса контейнеров**:
   ```bash
   docker compose ps
   docker stats
   ```

### Резервное копирование

Настройте автоматические бэкапы (см. [DOCKER_COMPOSE_GUIDE.md](DOCKER_COMPOSE_GUIDE.md))

## 📞 Поддержка Reg.ru

Если возникнут проблемы с сервером:
- Техподдержка: [support.reg.ru](https://www.reg.ru/support/)
- Документация: [www.reg.ru/support/help/cloud](https://www.reg.ru/support/help/cloud)

## ✅ Чеклист после покупки

- [ ] Сервер создан и доступен
- [ ] Подключение по SSH работает
- [ ] Docker установлен
- [ ] Docker Compose установлен
- [ ] Firewall настроен
- [ ] Проект склонирован
- [ ] Переменные окружения настроены
- [ ] Деплой выполнен успешно
- [ ] Домен настроен (если используется)
- [ ] SSL сертификат установлен (если используется домен)

## 🚀 Готово!

После выполнения всех шагов ваш проект будет доступен по IP адресу или домену.

Следуйте инструкциям в:
- [REG_RU_QUICK_START.md](REG_RU_QUICK_START.md) - быстрый старт
- [DOCKER_COMPOSE_GUIDE.md](DOCKER_COMPOSE_GUIDE.md) - детальная инструкция

