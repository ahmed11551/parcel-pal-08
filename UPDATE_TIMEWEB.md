# 🔄 Обновление проекта на Timeweb

Инструкция по обновлению существующего проекта на Timeweb Cloud.

**Ваш сервер:** `89.169.1.238`  
**Домен:** `sendbuddy.ru`

---

## 🔄 Быстрое обновление Backend

### Шаг 1: Подключитесь к серверу

```bash
ssh root@89.169.1.238
```

### Шаг 2: Обновите код Backend

```bash
# Перейдите в папку проекта
cd /root/parcel-pal-08/backend
# ИЛИ
cd /root/sendbuddy/backend
# ИЛИ где у вас находится проект

# Обновите код из GitHub
git pull origin main

# Убедитесь, что .env файл на месте
ls -la .env
```

### Шаг 3: Перезапустите Backend

**Если используете Docker:**

```bash
# Пересоберите образ
docker build -t sendbuddy-backend:latest .

# Остановите старый контейнер
docker stop sendbuddy-backend
docker rm sendbuddy-backend

# Запустите новый
docker run -d \
    --name sendbuddy-backend \
    --restart unless-stopped \
    -p 3001:3001 \
    --env-file .env \
    sendbuddy-backend:latest

# Проверьте логи
docker logs -f sendbuddy-backend
```

**ИЛИ используйте скрипт:**

```bash
cd /root/parcel-pal-08
chmod +x deploy-timeweb.sh
./deploy-timeweb.sh backend
```

**Если запускаете через npm/pm2:**

```bash
cd backend
npm install
npm run build
pm2 restart sendbuddy-backend
# ИЛИ
pm2 restart all
```

---

## 🎨 Обновление Frontend

### Вариант 1: На том же сервере (Nginx)

```bash
# Подключитесь к серверу
ssh root@89.169.1.238

# Перейдите в папку frontend
cd /var/www/sendbuddy-frontend
# ИЛИ
cd /root/parcel-pal-08

# Обновите код
git pull origin main

# Установите зависимости (если нужно)
npm install

# Соберите проект
npm run build

# Перезагрузите Nginx
systemctl reload nginx
```

### Вариант 2: Через скрипт

```bash
cd /root/parcel-pal-08
chmod +x deploy-frontend-timeweb.sh
./deploy-frontend-timeweb.sh
```

### Вариант 3: На Vercel (если используете)

Просто сделайте `git push` - Vercel автоматически обновит!

---

## 🔍 Проверка работы

### 1. Проверьте Backend

```bash
# На сервере
curl http://localhost:3001/api/docs

# Или в браузере
https://sendbuddy.ru/api/docs
```

### 2. Проверьте Frontend

Откройте в браузере: `https://sendbuddy.ru`

---

## 📝 Что изменилось

После `git pull` вы получите:

1. ✅ **Поддержка DATABASE_URL** в `database.config.ts` - теперь работает с Railway и другими платформами
2. ✅ **Обновленная валидация** в `main.ts` - поддерживает разные варианты env переменных
3. ✅ **Обновленный vercel.json** - убран прокси (если используете Vercel)
4. ✅ **Новые инструкции** по деплою

**Эти изменения обратно совместимы** - если у вас уже настроен `.env` с отдельными переменными (DB_HOST, DB_PORT и т.д.), все будет работать как раньше!

---

## ⚠️ Важно

**Не нужно менять `.env` файл!**

Если у вас уже настроен `.env` с переменными:
```env
DB_HOST=xxx.timeweb.cloud
DB_PORT=5432
DB_USERNAME=sendbuddy
DB_PASSWORD=xxx
DB_DATABASE=sendbuddy
```

То все будет работать как раньше. Новая поддержка `DATABASE_URL` - это дополнительная возможность, но не обязательная.

---

## 🐛 Если что-то пошло не так

### Backend не запускается

```bash
# Проверьте логи
docker logs sendbuddy-backend
# ИЛИ
pm2 logs sendbuddy-backend

# Проверьте .env файл
cat backend/.env

# Проверьте подключение к БД
docker exec -it sendbuddy-backend sh
# Внутри контейнера проверьте переменные
```

### Frontend не работает

```bash
# Проверьте Nginx логи
tail -f /var/log/nginx/error.log

# Проверьте статус Nginx
systemctl status nginx

# Проверьте сборку
cd /var/www/sendbuddy-frontend
npm run build
```

---

## ✅ Готово!

После обновления ваш проект будет работать с новыми улучшениями, сохраняя всю существующую конфигурацию.

**URLs:**
- Frontend: `https://sendbuddy.ru`
- Backend API: `https://sendbuddy.ru/api` или `http://89.169.1.238:3001/api`
- API Docs: `https://sendbuddy.ru/api/docs`

---

🚀 **Удачи с обновлением!**

