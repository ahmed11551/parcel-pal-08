# 🔧 Исправление: Frontend контейнер не запущен

## ❌ Проблема

Frontend контейнер отсутствует в списке запущенных контейнеров:

```bash
docker compose ps
# Нет sendbuddy-frontend
```

---

## 🔍 Диагностика

### 1. Проверьте что frontend пытался запуститься:

```bash
# Проверьте все контейнеры (включая остановленные)
docker compose ps -a

# Или через docker напрямую
docker ps -a | grep frontend
```

### 2. Проверьте логи сборки и запуска:

```bash
# Попробуйте запустить frontend отдельно
docker compose up frontend

# Это покажет ошибки при запуске
```

### 3. Проверьте что Dockerfile.frontend существует:

```bash
ls -la Dockerfile.frontend
```

---

## ✅ Решения

### Решение 1: Запустить frontend

```bash
cd /root/parcel-pal-08

# Запустить только frontend
docker compose up -d frontend

# Проверить статус
docker compose ps frontend
```

### Решение 2: Пересобрать frontend

```bash
cd /root/parcel-pal-08

# Остановить все
docker compose down

# Пересобрать frontend
docker compose build --no-cache frontend

# Запустить все
docker compose up -d

# Проверить статус
docker compose ps
```

### Решение 3: Проверить ошибки сборки

```bash
cd /root/parcel-pal-08

# Попробовать собрать frontend с выводом логов
docker compose build frontend

# Если есть ошибки - они будут видны
```

### Решение 4: Проверить зависимости

Frontend зависит от backend (должен быть healthy):

```bash
# Проверьте что backend здоров
docker compose ps backend

# Должно быть: (healthy)

# Если backend не healthy, исправьте сначала его
docker compose logs backend --tail=50
```

---

## 🚀 Пошаговая инструкция

### Шаг 1: Проверьте все контейнеры

```bash
docker compose ps -a
```

### Шаг 2: Попробуйте запустить frontend

```bash
docker compose up -d frontend
```

### Шаг 3: Проверьте логи если не запускается

```bash
# Смотрите логи в реальном времени
docker compose up frontend

# Или проверьте последние логи
docker compose logs frontend --tail=100
```

### Шаг 4: Если есть ошибки сборки - пересоберите

```bash
docker compose build --no-cache frontend
docker compose up -d frontend
```

### Шаг 5: Проверьте что frontend запустился

```bash
docker compose ps frontend

# Должно быть: Up (healthy)

# Проверьте что отвечает
curl -I http://localhost:3000
```

---

## 🔍 Частые причины

1. **Ошибка при сборке** - проверьте `docker compose build frontend`
2. **Нет Dockerfile.frontend** - проверьте что файл существует
3. **Ошибка зависимостей** - frontend не может запуститься если backend не healthy
4. **Нехватка памяти** - проверьте `free -h` и `docker stats`
5. **Порт 3000 занят** - проверьте `netstat -tlnp | grep 3000`

---

## ⚠️ Если порт 3000 занят

```bash
# Проверьте что использует порт 3000
sudo lsof -i :3000
# или
sudo netstat -tlnp | grep 3000

# Остановите процесс или измените порт в docker-compose.yml
```

---

## ✅ После исправления

1. Проверьте что frontend запущен: `docker compose ps`
2. Проверьте что отвечает: `curl http://localhost:3000`
3. Проверьте сайт: `https://send-buddy.ru`

---

## 📋 Быстрая команда

```bash
cd /root/parcel-pal-08

# Полная перезагрузка
docker compose down
docker compose build frontend
docker compose up -d

# Подождите 30 секунд
sleep 30

# Проверьте
docker compose ps
curl -I http://localhost:3000
```

