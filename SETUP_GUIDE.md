# Руководство по настройке и запуску SendBuddy

## 📋 Предварительные требования

- Node.js 18+ и npm
- PostgreSQL 14+
- Git

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Frontend
npm install

# Backend
cd backend
npm install
cd ..
```

### 2. Настройка базы данных

1. Установите и запустите PostgreSQL
2. Создайте базу данных:

```sql
CREATE DATABASE sendbuddy;
```

### 3. Настройка Backend

1. Создайте файл `backend/.env`:

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=sendbuddy

# JWT
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# Application
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:5173

# SMS Service (опционально, для продакшена)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=your-twilio-phone-number

# Payment Systems (опционально)
YOOKASSA_SHOP_ID=your-yookassa-shop-id
YOOKASSA_SECRET_KEY=your-yookassa-secret-key
PAYMENT_RETURN_URL=http://localhost:5173/payment/success

# File Storage (опционально, для продакшена)
S3_ACCESS_KEY_ID=your-s3-access-key
S3_SECRET_ACCESS_KEY=your-s3-secret-key
S3_REGION=ru-central1
S3_ENDPOINT=https://storage.yandexcloud.net
S3_BUCKET_NAME=sendbuddy-files
S3_PUBLIC_URL=https://storage.yandexcloud.net/sendbuddy-files
```

2. Запустите backend:

```bash
cd backend
npm run start:dev
```

Backend будет доступен на `http://localhost:3001`
Swagger документация: `http://localhost:3001/api/docs`

### 4. Настройка Frontend

1. Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:3001/api
```

2. Запустите frontend:

```bash
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## 🧪 Тестирование

### Проверка работы

1. Откройте `http://localhost:5173`
2. Попробуйте зарегистрироваться:
   - Введите номер телефона
   - В dev режиме код будет в консоли backend
   - Введите код для входа

3. Создайте задание:
   - Перейдите в "Создать задание"
   - Заполните форму
   - Загрузите фото
   - Отправьте на модерацию

4. Просмотрите задания:
   - Перейдите в "Задания"
   - Используйте фильтры

## 📁 Структура проекта

```
SendBuddynew/
├── backend/              # NestJS Backend
│   ├── src/
│   │   ├── auth/        # Аутентификация
│   │   ├── users/       # Пользователи
│   │   ├── tasks/       # Задания
│   │   ├── orders/      # Заказы
│   │   ├── payments/    # Платежи
│   │   ├── chat/        # Чат
│   │   ├── reviews/     # Отзывы
│   │   ├── admin/       # Админ-панель
│   │   └── files/       # Загрузка файлов
│   └── .env             # Переменные окружения
├── src/                  # React Frontend
│   ├── pages/           # Страницы
│   ├── components/      # Компоненты
│   ├── contexts/        # Context API
│   └── lib/             # Утилиты и API клиент
└── .env                 # Переменные окружения frontend
```

## 🔧 Команды

### Backend

```bash
cd backend

# Разработка
npm run start:dev

# Сборка
npm run build

# Продакшен
npm run start:prod

# Миграции
npm run migration:generate -- -n MigrationName
npm run migration:run
npm run migration:revert

# Линтинг
npm run lint
```

### Frontend

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Превью сборки
npm run preview

# Линтинг
npm run lint
```

## ⚠️ Важные замечания

### Для разработки

1. **SMS коды**: В dev режиме коды выводятся в консоль backend
2. **База данных**: Используется `synchronize: true` в dev (автоматическое создание таблиц)
3. **Файлы**: Загрузка файлов не будет работать без настройки S3 (можно добавить локальное хранение)

### Для продакшена

1. **JWT_SECRET**: Обязательно измените на случайную строку минимум 32 символа
2. **База данных**: Отключите `synchronize`, используйте миграции
3. **HTTPS**: Обязательно используйте HTTPS
4. **Переменные окружения**: Не коммитьте `.env` файлы
5. **SMS**: Настройте реальный SMS-сервис (Twilio)
6. **Платежи**: Настройте ЮKassa или другую платежную систему
7. **Файлы**: Настройте S3 или Yandex Object Storage

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте, что PostgreSQL запущен
2. Проверьте настройки в `.env`
3. Проверьте, что порт 3001 свободен

### Frontend не подключается к API

1. Проверьте `VITE_API_URL` в `.env`
2. Убедитесь, что backend запущен
3. Проверьте CORS настройки в backend

### Ошибки базы данных

1. Убедитесь, что база данных создана
2. Проверьте права доступа пользователя PostgreSQL
3. Проверьте настройки подключения в `.env`

## 📚 Дополнительная документация

- [IMPLEMENTATION.md](./IMPLEMENTATION.md) - Описание реализованного функционала
- [FRONTEND_COMPLETE.md](./FRONTEND_COMPLETE.md) - Описание frontend страниц
- [NEXT_STEPS.md](./NEXT_STEPS.md) - Следующие шаги развития
- [backend/README.md](./backend/README.md) - Документация backend

## 🎯 Следующие шаги

После успешного запуска:

1. Протестируйте основные сценарии:
   - Регистрация/вход
   - Создание задания
   - Отклик курьера
   - Выбор курьера
   - Оплата
   - Доставка
   - Отзывы

2. Настройте продакшен окружение:
   - Настройте реальные сервисы (SMS, платежи, хранилище)
   - Настройте CI/CD
   - Настройте мониторинг

3. Доработайте функционал:
   - WebSocket для чата
   - Пуш-уведомления
   - Интеграция с картами
   - И другие улучшения из NEXT_STEPS.md

