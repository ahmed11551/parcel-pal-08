# Настройка Telegram бота SendBuddyExpress_Bot

## ✅ Бот настроен и готов к работе!

**Название бота:** SendBuddyExpress_Bot  
**Токен:** Настроен в переменных окружения

## 🚀 Запуск бота

### На сервере (Docker Compose):

1. **Добавьте токен в `.env.production`:**
   ```bash
   nano .env.production
   ```
   
   Добавьте строку:
   ```env
   TELEGRAM_BOT_TOKEN=8146754886:AAF0KRnXaCU3RwwqLSR0YomJkFbG6UFx8l4
   ```

2. **Запустите бота:**
   ```bash
   docker compose up -d telegram-bot
   ```

3. **Проверьте статус:**
   ```bash
   docker compose ps telegram-bot
   docker compose logs telegram-bot
   ```

### Локально (для разработки):

1. **Создайте `.env` в папке `telegram-bot/`:**
   ```env
   TELEGRAM_BOT_TOKEN=8146754886:AAF0KRnXaCU3RwwqLSR0YomJkFbG6UFx8l4
   API_URL=http://localhost:3001/api
   MINI_APP_URL=http://localhost:8080
   ```

2. **Установите зависимости:**
   ```bash
   cd telegram-bot
   npm install
   ```

3. **Запустите:**
   ```bash
   npm run dev
   ```

## 📋 Доступные команды бота

- `/start` - Начать работу с ботом, открыть Mini App
- `/help` - Справка по командам
- `/support` - Связаться с поддержкой
- `/reviews` - Оставить отзыв
- `/tasks` - Открыть список заданий

## 🔧 Настройка Mini App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите бота **SendBuddyExpress_Bot**
4. Выберите **Bot Settings** → **Menu Button**
5. Выберите **Configure Menu Button**
6. Введите URL: `https://send-buddy.ru`
7. Готово! Теперь кнопка меню будет открывать ваше приложение

## 🔗 Интеграция с Backend

Бот автоматически:
- ✅ Отправляет уведомления о новых заданиях
- ✅ Уведомляет о назначении курьера
- ✅ Отправляет уведомления об изменении статуса
- ✅ Обрабатывает сообщения поддержки
- ✅ Собирает отзывы

## 📊 Мониторинг

### Проверка логов:
```bash
docker compose logs -f telegram-bot
```

### Проверка статуса:
```bash
docker compose ps telegram-bot
```

### Перезапуск бота:
```bash
docker compose restart telegram-bot
```

## 🔐 Безопасность

⚠️ **Важно:**
- Токен бота хранится в `.env.production` (не коммитится в Git)
- Бот работает в изолированной Docker сети
- Все запросы к API проходят через внутреннюю сеть

## 🐛 Решение проблем

### Бот не запускается:
1. Проверьте токен в `.env.production`
2. Проверьте логи: `docker compose logs telegram-bot`
3. Убедитесь, что backend запущен: `docker compose ps backend`

### Бот не отвечает:
1. Проверьте, что бот запущен: `docker compose ps telegram-bot`
2. Проверьте логи на ошибки
3. Убедитесь, что backend доступен: `curl http://localhost:3001/api/health`

### Уведомления не приходят:
1. Проверьте, что пользователи подписаны через Mini App
2. Проверьте логи backend на ошибки отправки уведомлений
3. Убедитесь, что таблица `telegram_notifications` создана

## 📝 Дополнительная настройка

### Webhook режим (для production с большей нагрузкой):

1. Настройте webhook URL в `.env.production`:
   ```env
   TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook/telegram
   ```

2. Настройте Nginx для проксирования webhook запросов

3. Перезапустите бота:
   ```bash
   docker compose restart telegram-bot
   ```

### Изменение интервала проверки уведомлений:

В `telegram-bot/src/index.ts` измените:
```typescript
startNotificationService(30000); // 30 секунд (по умолчанию)
```

## ✅ Готово!

Бот полностью настроен и готов к работе. После запуска он будет:
- Отвечать на команды
- Отправлять уведомления
- Обрабатывать сообщения поддержки
- Собирать отзывы

