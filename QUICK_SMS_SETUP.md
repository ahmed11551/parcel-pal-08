# Быстрая настройка SMS.ru для SendBuddy

## ✅ Ваш API ID получен!

**API ID:** `72E487A6-DFE3-ED3E-7C90-BD60FC7CBA72`

## 📝 Что нужно сделать на сервере:

### 1. Создайте файл `.env.production` на сервере:

```bash
cd /root/parcel-pal-08  # или путь к вашему проекту
nano .env.production
```

### 2. Добавьте следующие строки:

```env
# Database
DB_PASSWORD=ваш_пароль_бд

# JWT
JWT_SECRET=ваш_jwt_secret

# Frontend
FRONTEND_URL=https://send-buddy.ru
VITE_API_URL=https://send-buddy.ru/api

# SMS Provider (SMS.ru)
SMS_PROVIDER=smsru
SMSRU_API_ID=72E487A6-DFE3-ED3E-7C90-BD60FC7CBA72
```

### 3. Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano)

### 4. Перезапустите контейнеры:

```bash
docker compose down
docker compose up -d
```

### 5. Проверьте логи:

```bash
docker compose logs backend | grep SMS
```

Вы должны увидеть:
- `✅ SMS отправлен на ... через SMS.ru`
- `💰 Баланс SMS.ru: X руб.`

## ⚠️ Важно:

1. **Пополните баланс** на SMS.ru перед использованием
   - Войдите в личный кабинет: https://sms.ru
   - Перейдите в "Пополнение счета"
   - Пополните минимум на 100-300 рублей

2. **Не коммитьте `.env.production` в Git!**
   - Файл уже добавлен в `.gitignore`
   - Храните API ID в секрете

3. **Проверьте работу:**
   - Попробуйте зарегистрироваться
   - Код должен прийти на телефон через SMS
   - В логах будет показан баланс

## 🔍 Проверка работы:

После настройки попробуйте:
1. Открыть сайт
2. Перейти на страницу регистрации
3. Ввести номер телефона
4. Получить код (он придет на телефон)

Если код не приходит:
- Проверьте баланс на SMS.ru
- Проверьте логи: `docker compose logs backend`
- Убедитесь, что `SMS_PROVIDER=smsru` в `.env.production`

## 📞 Поддержка:

Если возникнут проблемы:
- Документация SMS.ru: https://sms.ru/api
- Телефон поддержки SMS.ru: +7 (800) 222-60-95

