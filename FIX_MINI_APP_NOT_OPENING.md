# 🔧 Исправление: Mini App не открывается в Telegram

## 🔍 Диагностика проблемы

Если приложение не открывается в Telegram, проверьте следующие моменты:

---

## 1️⃣ Проверьте настройки Menu Button в BotFather

### Способ 1: Через BotFather (рекомендуется)

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/mybots`
3. Выберите вашего бота **@SendBuddyExpress_Bot**
4. Выберите **Bot Settings**
5. Выберите **Menu Button**
6. Выберите **Configure Menu Button**
7. Введите текст кнопки: `Открыть SendBuddy`
8. Введите URL: `https://send-buddy.ru`
9. Сохраните

### Способ 2: Через API (быстро)

Выполните команду на сервере:

```bash
# На сервере
curl -X POST "https://api.telegram.org/bot8146754886:AAF0KRnXaCU3RwwqLSR0YomJkFbG6UFx8l4/setChatMenuButton" \
  -H "Content-Type: application/json" \
  -d '{
    "menu_button": {
      "type": "web_app",
      "text": "Открыть SendBuddy",
      "web_app": {
        "url": "https://send-buddy.ru"
      }
    }
  }'
```

**Важно:** Замените токен бота на ваш реальный токен!

---

## 2️⃣ Проверьте доступность домена

```bash
# Проверьте что сайт доступен
curl -I https://send-buddy.ru

# Должен вернуть 200 OK и HTTPS
```

**Если сайт не доступен:**
- Проверьте что frontend запущен на сервере
- Проверьте nginx конфигурацию
- Проверьте SSL сертификат

---

## 3️⃣ Проверьте SSL сертификат

Mini App **обязательно** требует HTTPS:

```bash
# Проверьте SSL
openssl s_client -connect send-buddy.ru:443 -servername send-buddy.ru < /dev/null 2>/dev/null | grep -i "verify return code"

# Должно быть: verify return code: 0 (ok)
```

---

## 4️⃣ Проверьте переменные окружения

### В docker-compose.yml:

```yaml
telegram-bot:
  environment:
    MINI_APP_URL: ${FRONTEND_URL:-https://send-buddy.ru}
```

### В .env.production на сервере:

```env
FRONTEND_URL=https://send-buddy.ru
MINI_APP_URL=https://send-buddy.ru
```

---

## 5️⃣ Проверьте что домен правильно настроен в коде

### В telegram-bot/src/index.ts:

```typescript
const MINI_APP_URL = process.env.MINI_APP_URL || 'https://send-buddy.ru';
```

### В telegram-bot/src/commands/start.ts:

```typescript
web_app: { url: MINI_APP_URL }
```

---

## 6️⃣ Проверьте кнопки в боте

При отправке `/start` должна быть кнопка с `web_app`:

```typescript
{
  text: '🚀 Открыть SendBuddy',
  web_app: { url: 'https://send-buddy.ru' }
}
```

---

## 7️⃣ Обновите бота после изменений

После изменения настроек перезапустите бота:

```bash
# На сервере
cd /root/parcel-pal-08
docker compose restart telegram-bot

# Или полный перезапуск
docker compose down
docker compose up -d telegram-bot
```

---

## 8️⃣ Проверьте логи бота

```bash
# Проверьте логи на ошибки
docker compose logs telegram-bot --tail=100

# Ищите ошибки связанные с web_app или Mini App
docker compose logs telegram-bot | grep -i "web_app\|mini_app\|error"
```

---

## 9️⃣ Тестирование

### Тест 1: Проверьте Menu Button

1. Откройте Telegram
2. Найдите бота **@SendBuddyExpress_Bot**
3. Внизу должна быть кнопка **"Открыть SendBuddy"**
4. Нажмите на неё
5. Должно открыться приложение

### Тест 2: Проверьте кнопку в /start

1. Отправьте `/start` боту
2. Должна быть кнопка **"🚀 Открыть SendBuddy"**
3. Нажмите на неё
4. Должно открыться приложение

---

## ❌ Частые ошибки и решения

### Ошибка 1: "Failed to open web app"

**Причина:** Неправильный URL или домен недоступен

**Решение:**
- Проверьте что URL правильный: `https://send-buddy.ru`
- Убедитесь что сайт доступен
- Проверьте SSL сертификат

### Ошибка 2: Кнопка Menu Button не появляется

**Причина:** Menu Button не настроен в BotFather

**Решение:**
- Настройте Menu Button через `/mybots` в BotFather
- Или используйте API команду выше

### Ошибка 3: Открывается, но показывает ошибку

**Причина:** Проблема с фронтендом или API

**Решение:**
- Проверьте логи frontend: `docker compose logs frontend`
- Проверьте логи backend: `docker compose logs backend`
- Проверьте консоль браузера в Mini App

### Ошибка 4: "This domain is not allowed"

**Причина:** Домен не добавлен в настройки бота

**Решение:**
- Убедитесь что используете правильный домен
- Проверьте настройки в BotFather
- Для тестирования можно использовать ngrok или другие туннели

---

## ✅ Быстрая проверка всех настроек

```bash
# 1. Проверьте доступность сайта
curl -I https://send-buddy.ru

# 2. Проверьте SSL
openssl s_client -connect send-buddy.ru:443 < /dev/null 2>/dev/null | grep "verify return code"

# 3. Проверьте настройки Menu Button (нужен токен)
curl -X GET "https://api.telegram.org/bot<TOKEN>/getChatMenuButton"

# 4. Установите Menu Button (нужен токен)
curl -X POST "https://api.telegram.org/bot<TOKEN>/setChatMenuButton" \
  -H "Content-Type: application/json" \
  -d '{"menu_button":{"type":"web_app","text":"Открыть SendBuddy","web_app":{"url":"https://send-buddy.ru"}}}'

# 5. Проверьте переменные окружения
docker compose exec telegram-bot printenv | grep MINI_APP_URL
```

---

## 🔧 Исправление проблемы (пошагово)

### Шаг 1: Настройте Menu Button

```bash
# На сервере (замените TOKEN на ваш токен)
TOKEN="8146754886:AAF0KRnXaCU3RwwqLSR0YomJkFbG6UFx8l4"

curl -X POST "https://api.telegram.org/bot${TOKEN}/setChatMenuButton" \
  -H "Content-Type: application/json" \
  -d '{
    "menu_button": {
      "type": "web_app",
      "text": "Открыть SendBuddy",
      "web_app": {
        "url": "https://send-buddy.ru"
      }
    }
  }'
```

### Шаг 2: Проверьте переменные окружения

```bash
# На сервере
cd /root/parcel-pal-08

# Проверьте .env.production
cat .env.production | grep FRONTEND_URL
cat .env.production | grep MINI_APP_URL
```

### Шаг 3: Обновите бота

```bash
# На сервере
cd /root/parcel-pal-08
git pull origin main
docker compose down
docker compose build --no-cache telegram-bot
docker compose up -d telegram-bot

# Проверьте логи
docker compose logs telegram-bot --tail=50
```

### Шаг 4: Протестируйте

1. Откройте Telegram
2. Найдите бота
3. Нажмите кнопку Menu Button внизу
4. Или отправьте `/start` и нажмите кнопку

---

## 📝 Чеклист

- [ ] Menu Button настроен в BotFather или через API
- [ ] URL правильный: `https://send-buddy.ru`
- [ ] Сайт доступен и работает на HTTPS
- [ ] SSL сертификат валидный
- [ ] `MINI_APP_URL` правильно настроен в docker-compose.yml
- [ ] Бот перезапущен после изменений
- [ ] Кнопка `/start` использует правильный URL
- [ ] Протестировано открытие через Menu Button
- [ ] Протестировано открытие через кнопку в /start

---

## 🆘 Если ничего не помогает

1. **Проверьте домен в BotFather:**
   - Откройте @BotFather
   - `/mybots` → Ваш бот → Bot Settings → Domain
   - Убедитесь что домен добавлен

2. **Используйте временный домен для тестирования:**
   - ngrok: `ngrok http 3000`
   - Используйте полученный HTTPS URL

3. **Проверьте версию Telegram:**
   - Убедитесь что у вас последняя версия Telegram
   - Mini App поддерживается начиная с определенной версии

4. **Проверьте логи фронтенда:**
   ```bash
   docker compose logs frontend --tail=100
   ```

---

## ✅ После исправления

После настройки Menu Button и перезапуска бота:

1. Откройте Telegram
2. Найдите бота **@SendBuddyExpress_Bot**
3. Внизу должна появиться кнопка **"Открыть SendBuddy"**
4. Нажмите на неё
5. Приложение должно открыться!

---

**Если проблема осталась, проверьте логи и сообщите об ошибке!**

