# 🌐 Подключение домена Timeweb к Vercel

## 📋 Пошаговая инструкция

### Шаг 1: Добавить домен в Vercel

1. Откройте проект на Vercel: https://vercel.com/dashboard
2. Выберите проект `parcel-pal-08` (или ваш проект)
3. Перейдите в **Settings** → **Domains**
4. Нажмите **Add Domain**
5. Введите ваш домен (например: `sendbuddy.ru` или `www.sendbuddy.ru`)
6. Нажмите **Add**

---

### Шаг 2: Получить DNS записи от Vercel

После добавления домена Vercel покажет необходимые DNS записи:

**Обычно это:**
- **A запись** или **CNAME запись** для основного домена
- **CNAME запись** для `www` поддомена

**Пример:**
```
Тип: A
Имя: @
Значение: 76.76.21.21

Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
```

---

### Шаг 3: Настроить DNS в Timeweb

#### Вариант A: Использовать DNS Timeweb (рекомендуется)

1. Войдите в панель Timeweb: https://timeweb.com/
2. Перейдите в **Домены** → выберите ваш домен
3. Откройте **DNS-записи** или **Управление DNS**
4. Добавьте записи, которые показал Vercel:

   **Для основного домена:**
   ```
   Тип: A
   Имя: @ (или оставьте пустым)
   Значение: IP адрес от Vercel (например: 76.76.21.21)
   TTL: 3600
   ```

   **Для www поддомена:**
   ```
   Тип: CNAME
   Имя: www
   Значение: cname.vercel-dns.com (или значение от Vercel)
   TTL: 3600
   ```

5. Сохраните изменения

#### Вариант B: Перенести DNS на Vercel (продвинутый)

1. В Vercel перейдите в **Settings** → **Domains** → ваш домен
2. Выберите **Use Vercel DNS**
3. Vercel покажет **Nameservers** (NS записи)
4. В Timeweb измените **Nameservers** на значения от Vercel

---

### Шаг 4: Ожидание распространения DNS

- **Обычно:** 5-30 минут
- **Максимум:** до 48 часов (редко)

Проверить статус можно:
- В Vercel: **Settings** → **Domains** → статус домена
- Через команду: `nslookup ваш-домен.ru`

---

### Шаг 5: SSL сертификат

Vercel **автоматически** выдаст SSL сертификат (HTTPS) после подключения домена.

Обычно это занимает несколько минут после настройки DNS.

---

## 🔧 Обновление конфигурации проекта

### 1. Обновите `vercel.json` (если нужно):

```json
{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://89.169.1.238:3001/api/:path*"
    }
  ],
  "headers": [
    {
      "source": "/api/:path*",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        }
      ]
    }
  ]
}
```

### 2. Обновите переменные окружения в Vercel:

1. В Vercel: **Settings** → **Environment Variables**
2. Обновите `FRONTEND_URL`:
   ```
   FRONTEND_URL=https://ваш-домен.ru
   ```

### 3. Обновите backend `.env`:

На сервере обновите:
```env
FRONTEND_URL=https://ваш-домен.ru
PAYMENT_RETURN_URL=https://ваш-домен.ru/payment/success
```

---

## ✅ Проверка работы

После настройки проверьте:

1. **Откройте домен в браузере:**
   ```
   https://ваш-домен.ru
   ```

2. **Проверьте HTTPS:**
   - Должен быть зеленый замочек
   - URL должен начинаться с `https://`

3. **Проверьте API:**
   - Откройте DevTools → Network
   - Проверьте, что запросы идут на ваш домен

---

## 🐛 Решение проблем

### Домен не подключается

1. **Проверьте DNS записи:**
   ```bash
   nslookup ваш-домен.ru
   dig ваш-домен.ru
   ```

2. **Убедитесь, что записи правильные:**
   - A запись должна указывать на IP от Vercel
   - CNAME для www должен указывать на Vercel

3. **Подождите:** DNS может распространяться до 48 часов

### SSL сертификат не выдается

1. Подождите 10-30 минут после настройки DNS
2. Проверьте, что DNS записи правильные
3. В Vercel: **Settings** → **Domains** → проверьте статус

### Ошибка "Domain not configured"

1. Убедитесь, что домен добавлен в Vercel
2. Проверьте, что DNS записи настроены правильно
3. Подождите распространения DNS

---

## 📝 Пример настройки DNS в Timeweb

**Скриншот настроек (текстовое описание):**

```
DNS записи для sendbuddy.ru:

1. A запись:
   Имя: @
   Тип: A
   Значение: 76.76.21.21
   TTL: 3600

2. CNAME запись:
   Имя: www
   Тип: CNAME
   Значение: cname.vercel-dns.com
   TTL: 3600
```

---

## 🎯 Итоговая конфигурация

После настройки у вас будет:

- ✅ Домен работает на Vercel
- ✅ HTTPS автоматически настроен
- ✅ Frontend доступен по вашему домену
- ✅ API проксируется через `/api/*`

---

## 📚 Полезные ссылки

- **Vercel Domains:** https://vercel.com/docs/concepts/projects/domains
- **Timeweb DNS:** https://timeweb.com/ru/help/dns-zones
- **Проверка DNS:** https://dnschecker.org/

---

## ⚠️ Важно

1. **Не удаляйте старые DNS записи** сразу — подождите, пока новый домен заработает
2. **Сохраните backup** текущих DNS записей
3. **SSL сертификат** выдается автоматически, но может занять время
4. **Backend** должен быть доступен по IP или отдельному поддомену

---

## 🔄 Альтернатива: Поддомен для API

Если хотите использовать поддомен для API (например, `api.sendbuddy.ru`):

1. В Timeweb добавьте A запись:
   ```
   Имя: api
   Тип: A
   Значение: 89.169.1.238 (IP вашего сервера)
   ```

2. Обновите `vercel.json`:
   ```json
   {
     "rewrites": [
       {
         "source": "/api/:path*",
         "destination": "https://api.sendbuddy.ru/api/:path*"
       }
     ]
   }
   ```

3. Настройте Nginx на сервере для работы с поддоменом

---

**Готово!** Ваш домен теперь работает на Vercel! 🎉

