# 🔄 Обновление авторизации через Telegram

## 📝 Что было изменено

1. **Backend** (`backend/src/routes/telegram.ts`):
   - Добавлен новый endpoint `/api/telegram/auth/simple` для простой авторизации через Telegram ID
   - Улучшена интеграция `/api/telegram/auth` для автоматического связывания аккаунтов
   - Добавлено логирование
   - Исправлена обработка ошибок

2. **Frontend** (`src/contexts/AuthContext.tsx`, `src/hooks/useTelegram.ts`):
   - Исправлено несоответствие ключей токена (`token` вместо `accessToken`)
   - Улучшена синхронизация авторизации между ботом и Mini App

3. **Telegram Bot** (`telegram-bot/src/`):
   - Обновлена команда `/start` для автоматической авторизации
   - Добавлен обработчик кнопки авторизации
   - Улучшена обработка ошибок

## 🚀 Инструкция по обновлению на сервере

### Вариант 1: Если используете Docker Compose (рекомендуется)

```bash
# 1. Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# 2. Перейдите в директорию проекта
cd ~/SendBuddynew  # или другая директория с проектом

# 3. Получите последние изменения из GitHub
git pull origin main  # или master, в зависимости от вашей ветки

# 4. Пересоберите и перезапустите контейнеры
docker compose down
docker compose build --no-cache backend telegram-bot
docker compose up -d

# 5. Проверьте логи
docker compose logs -f backend telegram-bot
```

### Вариант 2: Если используете отдельные сервисы

#### Backend (Node.js/Express):

```bash
# 1. Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# 2. Перейдите в директорию backend
cd ~/SendBuddynew/backend

# 3. Получите последние изменения
git pull origin main

# 4. Переустановите зависимости (если нужно)
npm install

# 5. Перезапустите сервис (зависит от вашей конфигурации)
# Если используете PM2:
pm2 restart sendbuddy-backend

# Если используете systemd:
sudo systemctl restart sendbuddy-backend

# Если запускаете напрямую:
npm run build
npm start
```

#### Telegram Bot:

```bash
# 1. Перейдите в директорию бота
cd ~/SendBuddynew/telegram-bot

# 2. Получите последние изменения
git pull origin main

# 3. Переустановите зависимости (если нужно)
npm install

# 4. Перезапустите бота
# Если используете PM2:
pm2 restart sendbuddy-bot

# Если используете systemd:
sudo systemctl restart sendbuddy-bot

# Если запускаете напрямую:
npm run build
npm start
```

#### Frontend:

```bash
# 1. Перейдите в директорию проекта
cd ~/SendBuddynew

# 2. Получите последние изменения
git pull origin main

# 3. Пересоберите фронтенд
npm install
npm run build

# 4. Скопируйте в директорию веб-сервера (если нужно)
# Например, для nginx:
sudo cp -r dist/* /var/www/send-buddy.ru/

# 5. Перезапустите nginx (если нужно)
sudo systemctl reload nginx
```

### Вариант 3: Если используете Vercel/Railway/другие платформы

#### Vercel (Frontend):
```bash
# Просто задеплойте через Vercel CLI или GitHub integration
vercel --prod
```

#### Railway/Render (Backend):
- Обычно автоматически деплоится при push в GitHub
- Или используйте их CLI для ручного деплоя

## ✅ Проверка после обновления

1. **Проверьте работу бота:**
   ```bash
   # Откройте бота в Telegram и напишите /start
   # Должна произойти автоматическая авторизация
   ```

2. **Проверьте логи backend:**
   ```bash
   docker compose logs backend | grep -i "telegram\|auth"
   # или
   pm2 logs sendbuddy-backend
   ```

3. **Проверьте логи бота:**
   ```bash
   docker compose logs telegram-bot
   # или
   pm2 logs sendbuddy-bot
   ```

4. **Проверьте работу API:**
   ```bash
   curl https://your-domain.com/api/health
   curl https://your-domain.com/api/telegram/auth/simple \
     -X POST \
     -H "Content-Type: application/json" \
     -d '{"telegramId": 123456789, "firstName": "Test"}'
   ```

5. **Проверьте Mini App:**
   - Откройте Mini App через бота
   - Проверьте, что авторизация работает автоматически

## 🔍 Устранение проблем

### Если бот не запускается:
```bash
# Проверьте переменные окружения
cat telegram-bot/.env
# Убедитесь, что установлены:
# - TELEGRAM_BOT_TOKEN
# - API_URL
# - MINI_APP_URL
```

### Если backend выдает ошибки:
```bash
# Проверьте логи
docker compose logs backend
# Ищите ошибки связанные с:
# - Базой данных
# - JWT_SECRET
# - Telegram API
```

### Если авторизация не работает:
1. Проверьте, что токен бота валидный
2. Проверьте, что API_URL в боте правильный
3. Проверьте логи обеих сервисов

## 📌 Важные замечания

- ⚠️ **Не забудьте сделать бэкап базы данных перед обновлением** (если это возможно)
- ✅ **Обновление не требует миграций БД** - структура базы не изменилась
- ✅ **Обратная совместимость сохранена** - старые методы авторизации продолжают работать
- 🔒 **Безопасность**: Токены JWT используют тот же секрет, что и раньше

## 🎯 Что должно работать после обновления

1. ✅ Пользователь пишет `/start` в боте → автоматическая авторизация
2. ✅ Пользователь открывает Mini App → автоматическая авторизация (если авторизован в боте)
3. ✅ Токены синхронизируются между ботом и веб-приложением
4. ✅ Все функции бота работают (подписки, отзывы, поддержка)

