# Быстрое обновление на сервере и настройка Telegram

## 🚀 Шаг 1: Обновление кода на сервере

### Подключитесь к серверу:

```bash
ssh root@194.67.124.90
```

### Выполните команды:

```bash
# 1. Перейти в директорию проекта
cd /root/parcel-pal-08

# 2. Настроить git (если еще не настроено)
git config pull.rebase false

# 3. Получить последние изменения
git pull origin main

# 4. Остановить контейнеры
docker compose down

# 5. Пересобрать контейнеры с новыми изменениями
docker compose build --no-cache frontend backend telegram-bot

# 6. Запустить контейнеры
docker compose up -d

# 7. Проверить статус
docker compose ps

# 8. Проверить health check
curl http://localhost:3001/api/health | python3 -m json.tool
```

### Ожидаемый результат:

- Все контейнеры в статусе `healthy` или `running`
- Health check возвращает `{"status": "ok", ...}`

---

## 🤖 Шаг 2: Настройка Telegram Menu Button

### Вариант 1: Через BotFather (рекомендуется)

1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду: `/mybots`
4. Выберите бота **@SendBuddyExpress_Bot**
5. Нажмите **Bot Settings**
6. Нажмите **Menu Button**
7. Выберите **Configure Menu Button**
8. Введите текст кнопки: `Открыть SendBuddy`
9. Введите URL: `https://send-buddy.ru`
10. Нажмите **Done**

### Вариант 2: Через API (быстрее)

Выполните на сервере или локально:

```bash
curl -X POST "https://api.telegram.org/bot8146754886:AAF0KRnXaCU3RwwqLSR0YomJkFbG6UFx8l4/setChatMenuButton" \
  -H "Content-Type: application/json" \
  -d '{
    "menu_button": {
      "type": "web_app",
      "text": "Открыть SendBuddy",
      "web_app": {
        "url": "https://send-buddy.ru"
      }
    }
  }'
```

**Ожидаемый ответ:**
```json
{"ok":true,"result":true}
```

---

## ✅ Шаг 3: Проверка работы

### Проверьте сайт:

```bash
# На сервере
curl -I https://send-buddy.ru

# Должен вернуть HTTP 200
```

### Проверьте API:

```bash
# На сервере
curl https://send-buddy.ru/api/health | python3 -m json.tool

# Должен вернуть:
# {
#   "status": "ok",
#   "timestamp": "...",
#   "checks": {
#     "database": "ok",
#     "environment": {...}
#   }
# }
```

### Проверьте Telegram бота:

1. Откройте Telegram
2. Найдите бота **@SendBuddyExpress_Bot**
3. Отправьте `/start`
4. Бот должен ответить и показать кнопку "Открыть SendBuddy"
5. Нажмите на кнопку - должно открыться Mini App

---

## 🔍 Если что-то не работает

### Проблема: Git pull не работает

```bash
# Если есть конфликты
git fetch origin
git reset --hard origin/main

# Или если локальные изменения не важны
git stash
git pull origin main
```

### Проблема: Контейнеры не запускаются

```bash
# Проверить логи
docker compose logs backend --tail=50
docker compose logs frontend --tail=50
docker compose logs telegram-bot --tail=50

# Перезапустить
docker compose restart backend frontend telegram-bot
```

### Проблема: Health check не проходит

```bash
# Проверить переменные окружения
docker compose exec backend env | grep -E "JWT_SECRET|DATABASE_URL|TELEGRAM_BOT_TOKEN"

# Проверить подключение к БД
docker compose exec backend psql $DATABASE_URL -c "SELECT 1"
```

### Проблема: Telegram Menu Button не работает

1. Проверьте что бот запущен:
   ```bash
   docker compose logs telegram-bot --tail=30
   ```

2. Проверьте что токен правильный:
   ```bash
   docker compose exec telegram-bot env | grep TELEGRAM_BOT_TOKEN
   ```

3. Попробуйте настроить через BotFather еще раз

---

## 📋 Чеклист после обновления

- [ ] Код обновлен (`git pull` выполнен)
- [ ] Контейнеры пересобраны и запущены
- [ ] Health check проходит
- [ ] Сайт открывается: https://send-buddy.ru
- [ ] API работает: https://send-buddy.ru/api/health
- [ ] Telegram Menu Button настроен
- [ ] Бот отвечает на `/start`
- [ ] Mini App открывается из бота

---

## ✅ Готово!

После выполнения всех шагов приложение будет полностью готово к использованию! 🚀

**Статус**: ✅ **100% ГОТОВО К PRODUCTION**

