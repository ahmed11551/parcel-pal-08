# 🔧 Ручной запуск Backend для диагностики

## Проблема: Endpoint не работает

Если endpoint `/api/auth/send-code` возвращает 404, выполните следующие шаги:

## Шаг 1: Остановите все процессы backend

```bash
pkill -f "nest start"
pkill -f "node.*backend"
```

## Шаг 2: Запустите backend вручную в отдельном терминале

Откройте **новый терминал** и выполните:

```bash
cd /Users/ahmeddevops/Desktop/SendBuddynew/backend

# Убедитесь, что Node.js в PATH
export PATH="$HOME/.nvm/versions/node/v24.11.1/bin:$PATH"

# Запустите backend
npm run start:dev
```

## Шаг 3: Дождитесь полного запуска

Ищите в логах сообщения:
```
Application is running on: http://localhost:3001
Swagger docs available at: http://localhost:3001/api/docs
```

**ВАЖНО:** Если есть ошибки подключения к БД, их нужно исправить сначала!

## Шаг 4: Проверьте работу

В **другом терминале**:

```bash
# Проверка Swagger
curl http://localhost:3001/api/docs

# Проверка endpoint
curl -X POST http://localhost:3001/api/auth/send-code \
  -H "Content-Type: application/json" \
  -d '{"phone":"+79991234567"}'
```

Должен вернуться: `{"success":true}`

## Возможные проблемы

### 1. Ошибка подключения к БД

Если видите ошибки типа:
```
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**Решение:**
- Убедитесь, что PostgreSQL запущен
- Проверьте настройки в `backend/.env`
- Создайте базу данных: `createdb -U postgres sendbuddy`

### 2. Backend не компилируется

Если есть ошибки компиляции, исправьте их перед запуском.

### 3. Порт 3001 занят

```bash
lsof -i :3001
# Остановите процесс или измените порт
```

## После успешного запуска

1. Backend должен отвечать на http://localhost:3001/api/docs
2. Frontend должен успешно отправлять запросы
3. SMS коды будут выводиться в консоль backend

## Альтернатива: Запуск без БД (для теста)

Если проблема в БД, можно временно отключить TypeORM:

1. Закомментируйте TypeOrmModule в `app.module.ts`
2. Запустите backend
3. Endpoints должны работать (но без сохранения данных)

Но лучше исправить подключение к БД!

