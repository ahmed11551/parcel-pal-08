# 🔧 Как обновить сервер - пошаговая инструкция

## ⚠️ ВАЖНО: Команды выполняются на СЕРВЕРЕ, не на локальном компьютере!

---

## Шаг 1: Подключитесь к серверу

На вашем **локальном Mac** выполните:

```bash
ssh root@194.67.124.90
```

Введите пароль когда попросит.

---

## Шаг 2: Перейдите в директорию проекта

После подключения к серверу выполните:

```bash
cd /root/parcel-pal-08
```

---

## Шаг 3: Получите последние изменения

```bash
git pull origin main
```

---

## Шаг 4: Узнайте ваш Telegram ID

**На вашем телефоне в Telegram:**

1. Откройте бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему любое сообщение
3. Он покажет ваш ID (например: `123456789`)

**Или через веб-версию Telegram:**
- Откройте https://web.telegram.org
- Найдите бота @userinfobot
- Он покажет ваш ID

---

## Шаг 5: Добавьте ваш Telegram ID в .env.production

**На сервере** (после `cd /root/parcel-pal-08`):

```bash
# Замените 123456789 на ваш реальный Telegram ID
echo "ADMIN_TELEGRAM_IDS=123456789" >> .env.production
```

**Или отредактируйте файл вручную:**

```bash
nano .env.production
```

Добавьте строку:
```
ADMIN_TELEGRAM_IDS=123456789
```

Сохраните: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

---

## Шаг 6: Пересоберите и перезапустите telegram-bot

```bash
# Остановите контейнеры
docker compose down

# Пересоберите telegram-bot
docker compose build --no-cache telegram-bot

# Запустите контейнеры
docker compose up -d
```

---

## Шаг 7: Проверьте логи

```bash
docker compose logs telegram-bot --tail=30
```

Должны увидеть что-то вроде:
```
✅ Бот запущен (Long Polling)
✅ SendBuddyExpress_Bot готов к работе!
```

---

## Шаг 8: Проверьте работу команды /stats

**В Telegram боте:**

1. Откройте ваш бот [@SendBuddyExpress_Bot](https://t.me/SendBuddyExpress_Bot)
2. Отправьте команду `/stats`
3. Должна появиться статистика подписок

---

## 🔍 Проверка что все работает:

### Проверить статус контейнеров:
```bash
docker compose ps
```

Все должны быть `Up` и `healthy`.

### Проверить что переменная установлена:
```bash
docker compose exec telegram-bot env | grep ADMIN_TELEGRAM_IDS
```

Должна показать ваш Telegram ID.

### Проверить API статистики:
```bash
curl http://localhost:3001/api/telegram/subscribers?stats=true
```

Должен вернуть JSON со статистикой.

---

## ❌ Если что-то не работает:

### Ошибка "cd: no such file or directory":
- Вы не на сервере, а на локальной машине
- Подключитесь через SSH: `ssh root@194.67.124.90`

### Ошибка "fatal: not a git repository":
- Вы не в правильной директории
- Выполните: `cd /root/parcel-pal-08`

### Ошибка "no configuration file provided":
- Это ошибка docker-compose
- Проверьте что вы в директории с `docker-compose.yml`
- Выполните: `ls -la` (должен быть файл `docker-compose.yml`)

### Команда /stats не работает:
- Проверьте что `ADMIN_TELEGRAM_IDS` установлен правильно
- Проверьте что вы используете правильный Telegram ID
- Перезапустите бота: `docker compose restart telegram-bot`

---

## 📝 Краткая шпаргалка:

```bash
# 1. Подключиться к серверу (на локальном Mac)
ssh root@194.67.124.90

# 2. Перейти в проект (на сервере)
cd /root/parcel-pal-08

# 3. Обновить код
git pull origin main

# 4. Добавить Telegram ID (замените на ваш)
echo "ADMIN_TELEGRAM_IDS=123456789" >> .env.production

# 5. Пересобрать
docker compose down
docker compose build --no-cache telegram-bot
docker compose up -d

# 6. Проверить логи
docker compose logs telegram-bot --tail=30
```

---

## ✅ Готово!

После выполнения всех шагов команда `/stats` должна работать в боте.

