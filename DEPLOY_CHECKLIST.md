# ✅ Чеклист деплоя на Railway + Vercel

Быстрая памятка для деплоя.

---

## 🎯 Шаг 1: Railway (Backend)

### Регистрация и создание проекта
- [ ] Зарегистрироваться на [railway.app](https://railway.app)
- [ ] Создать "New Project"
- [ ] Подключить GitHub репозиторий
- [ ] Добавить сервис из GitHub (выбрать репозиторий, указать Root Directory: `backend`)

### PostgreSQL база данных
- [ ] Добавить PostgreSQL: "New" → "Database" → "Add PostgreSQL"
- [ ] Записать или скопировать переменные окружения из PostgreSQL сервиса

### Переменные окружения в Backend
Добавить в Backend сервисе (Variables):

**Обязательные:**
- [ ] `NODE_ENV=production`
- [ ] `PORT=3001`
- [ ] `JWT_SECRET=<случайная_строка_32+_символов>`
- [ ] `FRONTEND_URL=<будет_позже_из_Vercel>`

**База данных (вариант 1 - проще):**
- [ ] `DATABASE_URL=${{Postgres.DATABASE_URL}}` (если Railway создал эту переменную)

**База данных (вариант 2 - если нет DATABASE_URL):**
- [ ] `DB_HOST=${{Postgres.PGHOST}}`
- [ ] `DB_PORT=${{Postgres.PGPORT}}`
- [ ] `DB_DATABASE=${{Postgres.PGDATABASE}}`
- [ ] `DB_USERNAME=${{Postgres.PGUSER}}`
- [ ] `DB_PASSWORD=${{Postgres.PGPASSWORD}}`

**Опциональные (можно добавить позже):**
- [ ] `TWILIO_ACCOUNT_SID=`
- [ ] `TWILIO_AUTH_TOKEN=`
- [ ] `YOOKASSA_SHOP_ID=`
- [ ] `YOOKASSA_SECRET_KEY=`

### Настройки деплоя
- [ ] Проверить: Build Command = `npm install && npm run build`
- [ ] Проверить: Start Command = `npm run start:prod`
- [ ] Проверить: Root Directory = `backend`

### Получить URL
- [ ] Записать Railway URL: `https://your-backend.up.railway.app`
- [ ] Проверить работу: открыть `https://your-backend.up.railway.app/api/docs`

---

## 🎨 Шаг 2: Vercel (Frontend)

### Регистрация и импорт
- [ ] Зарегистрироваться на [vercel.com](https://vercel.com)
- [ ] "Add New Project"
- [ ] Подключить GitHub репозиторий
- [ ] Настроить проект:
  - Framework: Vite
  - Root Directory: `./`
  - Build Command: `npm run build`
  - Output Directory: `dist`

### Переменные окружения
- [ ] Добавить: `VITE_API_URL=https://your-backend.up.railway.app/api`
  (заменить на реальный Railway URL!)

### Деплой
- [ ] Нажать "Deploy"
- [ ] Дождаться завершения сборки
- [ ] Записать Vercel URL: `https://your-app.vercel.app`

---

## 🔄 Шаг 3: Связать Frontend и Backend

### В Vercel
- [ ] Проверить/обновить `VITE_API_URL` на правильный Railway URL
- [ ] Если обновили - сделать "Redeploy"

### В Railway
- [ ] Обновить `FRONTEND_URL` на Vercel URL: `https://your-app.vercel.app`
- [ ] Railway автоматически перезапустит сервис

---

## ✅ Проверка

- [ ] Backend работает: `https://your-backend.up.railway.app/api/docs` открывается
- [ ] Frontend работает: `https://your-app.vercel.app` открывается
- [ ] Интеграция работает: можно зарегистрироваться, запросы идут на API

---

## 🐛 Если что-то не работает

### Backend не запускается
1. Проверить логи в Railway → Deployments → View Logs
2. Проверить все переменные окружения
3. Проверить, что PostgreSQL запущен

### Frontend не подключается к API
1. Проверить `VITE_API_URL` в Vercel
2. Проверить консоль браузера (F12)
3. Проверить CORS в backend (должен быть правильный `FRONTEND_URL`)

### База данных не подключается
1. Проверить, что PostgreSQL сервис запущен в Railway
2. Проверить переменные окружения (DB_HOST, DB_PORT, и т.д. или DATABASE_URL)
3. Проверить логи в Railway

---

## 📝 Готово!

**URLs:**
- Backend: `https://your-backend.up.railway.app`
- Frontend: `https://your-app.vercel.app`
- API Docs: `https://your-backend.up.railway.app/api/docs`

---

## 🔄 Обновление

После изменений кода:
- Просто сделайте `git push` - Railway и Vercel автоматически задеплоят!

---

**Удачи!** 🚀

