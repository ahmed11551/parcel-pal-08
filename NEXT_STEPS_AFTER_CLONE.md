# 🚀 Следующие шаги после клонирования

## ✅ Что уже сделано:

- ✅ Репозиторий клонирован
- ✅ Оптимизация для 2GB RAM выполнена
- ✅ Swap файл создан (2GB)

## 📋 Что делать дальше:

### 1. Создайте файл `.env.production`

```bash
cat > .env.production << 'EOF'
# Database
DB_PASSWORD=$(openssl rand -base64 24)

# JWT
JWT_SECRET=$(openssl rand -hex 32)

# Frontend
FRONTEND_URL=https://send-buddy.ru
VITE_API_URL=https://send-buddy.ru/api
EOF
```

**Или вручную:**

```bash
nano .env.production
```

И заполните:
```env
DB_PASSWORD=<сгенерируйте: openssl rand -base64 24>
JWT_SECRET=<сгенерируйте: openssl rand -hex 32>
FRONTEND_URL=https://send-buddy.ru
VITE_API_URL=https://send-buddy.ru/api
```

### 2. Сгенерируйте пароли

```bash
# Генерация пароля для БД
openssl rand -base64 24

# Генерация секрета для JWT
openssl rand -hex 32
```

Скопируйте результаты и вставьте в `.env.production`

### 3. Запустите деплой

```bash
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически:
- Проверит переменные окружения
- Остановит существующие контейнеры
- Соберет Docker образы
- Запустит все сервисы
- Проверит здоровье сервисов

### 4. Проверьте статус

```bash
# Статус контейнеров
docker compose ps

# Логи (если нужно)
docker compose logs -f

# Проверка здоровья
curl http://localhost:3001/api/health
```

### 5. Настройте домен (после деплоя)

```bash
chmod +x setup-domain.sh
./setup-domain.sh
```

## ⚠️ Важно

- **Сохраните пароли** из `.env.production` в безопасном месте!
- **Не коммитьте** `.env.production` в Git (он уже в .gitignore)
- **Подождите** пока контейнеры запустятся (1-2 минуты)

## 🔍 Проверка работы

После деплоя проверьте:

```bash
# Frontend (должен быть доступен)
curl -I http://localhost:3000

# Backend API (должен вернуть {"status":"ok"})
curl http://localhost:3001/api/health

# База данных (должна работать)
docker compose exec postgres pg_isready -U sendbuddy_user
```

## ✅ Готово!

После успешного деплоя ваш проект будет доступен:
- Локально: http://194.67.124.90:3000
- После настройки домена: https://send-buddy.ru

