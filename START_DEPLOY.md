# 🚀 Начинаем деплой на Timeweb Cloud!

У вас есть данные для подключения к серверу:
- **IP адрес:** 89.169.1.238
- **Пароль:** t17u4q8qF_Cxi.

## ⚡ Быстрый старт

### Шаг 1: Подключитесь к серверу

```bash
ssh root@89.169.1.238
```

Когда попросит пароль, введите: `t17u4q8qF_Cxi.`

### Шаг 2: Запустите автоматическую установку

После подключения выполните:

```bash
# Скачайте скрипт установки
wget https://raw.githubusercontent.com/ahmed11551/parcel-pal-08/main/install-on-server.sh

# Сделайте его исполняемым
chmod +x install-on-server.sh

# Запустите
bash install-on-server.sh
```

Скрипт автоматически:
- ✅ Обновит систему
- ✅ Установит Docker
- ✅ Установит Git
- ✅ Клонирует проект
- ✅ Соберет и запустит Backend

### Шаг 3: Настройте .env файл

После установки скрипт попросит создать `.env` файл:

```bash
cd parcel-pal-08/backend
nano .env
```

**Вам нужно будет заполнить:**
1. Данные PostgreSQL (если еще не создали - создайте в панели Timeweb Cloud)
2. Данные Redis (если еще не создали - создайте в панели Timeweb Cloud)
3. Данные S3 (если еще не создали - создайте в панели Timeweb Cloud)
4. JWT_SECRET (можно сгенерировать: `openssl rand -base64 32`)

### Шаг 4: Запустите Backend

После настройки .env:

```bash
# Если скрипт не запустил автоматически
docker build -t sendbuddy-backend .
docker run -d \
  --name sendbuddy-backend \
  --restart unless-stopped \
  -p 3001:3001 \
  --env-file .env \
  sendbuddy-backend
```

### Шаг 5: Проверьте работу

```bash
# Проверьте логи
docker logs -f sendbuddy-backend

# Проверьте API (в другом терминале)
curl http://89.169.1.238:3001/api/docs
```

## 📋 Что нужно подготовить в Timeweb Cloud

Если еще не создали, создайте в панели:

1. **PostgreSQL база данных:**
   - "Облачные базы данных" → "Создать базу"
   - PostgreSQL 16, 2 CPU, 4 GB RAM
   - Запишите: хост, порт, пользователь, пароль

2. **Redis:**
   - "Облачные базы данных" → "Создать базу"
   - Redis 7, 1 CPU, 2 GB RAM
   - Запишите: хост, порт, пароль

3. **S3 Bucket:**
   - "Объектное хранилище S3" → "Создать бакет"
   - Название: `sendbuddy-files`
   - Создайте ключи доступа

4. **Важно:** Добавьте IP `89.169.1.238` в whitelist БД!

## 🔧 Если что-то пошло не так

```bash
# Проверьте логи
docker logs sendbuddy-backend

# Перезапустите контейнер
docker restart sendbuddy-backend

# Проверьте статус
docker ps
```

## ✅ Готово!

После успешного запуска ваш API будет доступен по адресу:
- **API Docs:** http://89.169.1.238:3001/api/docs
- **API:** http://89.169.1.238:3001/api

---

**Начните с подключения:**
```bash
ssh root@89.169.1.238
```

