# 🚀 Быстрый старт SendBuddy

## Шаг 1: Проверка зависимостей

Убедитесь, что установлены:
- Node.js 18+ (`node --version`)
- npm (`npm --version`)
- PostgreSQL 14+ (`psql --version`)

## Шаг 2: Установка зависимостей

```bash
# Frontend зависимости
npm install

# Backend зависимости
cd backend
npm install
cd ..
```

## Шаг 3: Настройка базы данных

1. Запустите PostgreSQL
2. Создайте базу данных:

```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE sendbuddy;

# Выйдите
\q
```

Или через командную строку:
```bash
createdb -U postgres sendbuddy
```

## Шаг 4: Настройка переменных окружения

✅ Файлы `.env` уже созданы:
- `backend/.env` - для backend
- `.env` - для frontend

При необходимости отредактируйте настройки базы данных в `backend/.env`:
```env
DB_USERNAME=postgres
DB_PASSWORD=ваш_пароль
```

## Шаг 5: Запуск проекта

### Вариант 1: Запуск в отдельных терминалах

**Терминал 1 - Backend:**
```bash
cd backend
npm run start:dev
```

**Терминал 2 - Frontend:**
```bash
npm run dev
```

### Вариант 2: Использование npm-run-all (если установлен)

```bash
npm install -g npm-run-all
npm-run-all --parallel backend:dev frontend:dev
```

## Шаг 6: Проверка работы

1. **Backend**: Откройте http://localhost:3001/api/docs (Swagger документация)
2. **Frontend**: Откройте http://localhost:5173

## 🧪 Тестирование

### 1. Регистрация/Вход

1. Откройте http://localhost:5173
2. Нажмите "Войти"
3. Введите номер телефона (например: +79991234567)
4. В консоли backend вы увидите SMS код (в dev режиме)
5. Введите код для входа

### 2. Создание задания

1. После входа нажмите "Создать задание"
2. Заполните форму:
   - Загрузите фото посылки
   - Укажите название и описание
   - Выберите размер
   - Укажите аэропорты отправления и назначения
   - Укажите даты и вознаграждение
3. Нажмите "Опубликовать задание"
4. Задание будет отправлено на модерацию

### 3. Просмотр заданий

1. Перейдите в "Задания"
2. Используйте фильтры для поиска
3. Нажмите на задание для просмотра деталей

### 4. Отклик на задание (как курьер)

1. Откройте задание
2. Нажмите "Откликнуться на задание"
3. Напишите сообщение (опционально)
4. Отправьте отклик

### 5. Выбор курьера (как отправитель)

1. В детальном просмотре задания вы увидите список откликов
2. Нажмите "Выбрать" рядом с курьером
3. Курьер будет выбран, заказ перейдет в статус "Курьер выбран"

## ⚠️ Важные замечания

### Для разработки

- **SMS коды**: В dev режиме коды выводятся в консоль backend
- **База данных**: Автоматически создаются таблицы при первом запуске
- **Файлы**: Загрузка файлов не работает без настройки S3 (можно добавить локальное хранение позже)

### Возможные проблемы

1. **Backend не запускается**
   - Проверьте, что PostgreSQL запущен
   - Проверьте настройки в `backend/.env`
   - Проверьте, что порт 3001 свободен

2. **Ошибка подключения к БД**
   - Убедитесь, что PostgreSQL запущен
   - Проверьте правильность пароля в `backend/.env`
   - Убедитесь, что база данных `sendbuddy` создана

3. **Frontend не подключается к API**
   - Убедитесь, что backend запущен
   - Проверьте `VITE_API_URL` в `.env`
   - Проверьте CORS настройки

## 📚 Дополнительная документация

- [SETUP_GUIDE.md](./SETUP_GUIDE.md) - Подробное руководство по настройке
- [IMPLEMENTATION.md](./IMPLEMENTATION.md) - Описание функционала
- [FRONTEND_COMPLETE.md](./FRONTEND_COMPLETE.md) - Описание frontend
- [backend/README.md](./backend/README.md) - Документация API

## 🎯 Следующие шаги

После успешного запуска:
1. Протестируйте все основные сценарии
2. Настройте админ-панель (создайте пользователя с ролью admin)
3. Настройте реальные сервисы для продакшена (SMS, платежи, хранилище)

