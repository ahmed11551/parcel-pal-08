# Инструкция по деплою на сервер

## 🚀 Быстрое обновление на сервере

### Вариант 1: Автоматический (рекомендуется)

```bash
# Подключитесь к серверу
ssh root@194.67.124.90

# Перейдите в директорию проекта
cd ~/parcel-pal-08

# Скачайте последние изменения
git pull

# Запустите скрипт обновления
chmod +x UPDATE_SERVER.sh
./UPDATE_SERVER.sh
```

Скрипт автоматически:
- ✅ Получит последние изменения из GitHub
- ✅ Пересоберет Docker образы
- ✅ Перезапустит контейнеры
- ✅ Проверит работоспособность

### Вариант 2: Ручной

```bash
# 1. Подключитесь к серверу
ssh root@194.67.124.90

# 2. Перейдите в директорию проекта
cd ~/parcel-pal-08

# 3. Получите последние изменения
git pull

# 4. Остановите контейнеры
docker compose down

# 5. Пересоберите образы (если были изменения в коде)
docker compose build --no-cache

# 6. Запустите контейнеры
docker compose up -d

# 7. Проверьте статус
docker compose ps

# 8. Проверьте логи
docker compose logs -f
```

## 📝 Сохранение изменений в GitHub (с локального компьютера)

### 1. Проверьте изменения:

```bash
git status
```

### 2. Добавьте все изменения:

```bash
git add -A
```

### 3. Закоммитьте:

```bash
git commit -m "Описание изменений"
```

### 4. Отправьте в GitHub:

```bash
git push
```

## 🔄 Полный цикл обновления

### На локальном компьютере:

```bash
# 1. Внесите изменения в код
# ... редактируйте файлы ...

# 2. Сохраните в GitHub
git add -A
git commit -m "Описание изменений"
git push
```

### На сервере:

```bash
# 1. Подключитесь
ssh root@194.67.124.90

# 2. Обновите код
cd ~/parcel-pal-08
git pull

# 3. Перезапустите (автоматически)
./UPDATE_SERVER.sh

# Или вручную:
docker compose down
docker compose build --no-cache
docker compose up -d
```

## ⚠️ Важно

1. **Не коммитьте `.env.production`** - он уже в `.gitignore`
2. **Перед обновлением проверьте** что все работает локально
3. **После обновления проверьте логи**: `docker compose logs -f`
4. **Если что-то сломалось**, можно откатиться:
   ```bash
   git log  # Найдите нужный коммит
   git checkout <commit-hash>
   docker compose down
   docker compose build --no-cache
   docker compose up -d
   ```

## 📋 Проверка после обновления

```bash
# Статус контейнеров
docker compose ps

# Health endpoints
curl http://localhost:3001/api/health
curl http://localhost:3000/health

# Логи
docker compose logs --tail=50 backend
docker compose logs --tail=50 frontend
```

## 🆘 Если что-то пошло не так

1. **Проверьте логи**: `docker compose logs`
2. **Проверьте переменные окружения**: `cat .env.production`
3. **Проверьте статус**: `docker compose ps`
4. **Откатитесь к предыдущей версии** (см. выше)

