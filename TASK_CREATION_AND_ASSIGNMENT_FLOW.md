# Логика создания задания и принятия его курьером

## ✅ Проверка логики

### 1. Создание задания (Sender)

**Frontend**: `src/pages/CreateTask.tsx`
- Пользователь заполняет форму (3 шага)
- Вызывает `api.createTask(taskData)`

**Backend**: `backend/src/routes/tasks.ts` → `POST /api/tasks`
```typescript
router.post('/', authenticateToken, async (req: AuthRequest, res) => {
  // 1. Валидация данных через Zod схему
  const data = createTaskSchema.parse(req.body);
  
  // 2. Создание задания со статусом 'active'
  // 3. Создание записи в таблице payments со статусом 'pending'
  // 4. Отправка уведомлений подписанным пользователям
  
  // Результат: задание создано, статус = 'active', courier_id = NULL
})
```

**Результат:**
- ✅ Задание создано в БД со статусом `active`
- ✅ `courier_id = NULL` (курьер еще не назначен)
- ✅ Создана запись в `payments` для эскроу
- ✅ Отправлены уведомления подписанным пользователям

---

### 2. Просмотр заданий (Courier)

**Frontend**: `src/pages/Tasks.tsx`
- Курьер видит список активных заданий
- Может фильтровать по аэропортам отправления/назначения
- Вызывает `api.getTasks({ from, to, status: 'active' })`

**Backend**: `backend/src/routes/tasks.ts` → `GET /api/tasks`
```typescript
router.get('/', optionalAuth, async (req: AuthRequest, res) => {
  // Возвращает только задания со статусом 'active'
  // Фильтрует по from_airport и to_airport если указаны
})
```

**Результат:**
- ✅ Курьер видит только активные задания (status = 'active')
- ✅ Может фильтровать по маршруту
- ✅ Видит информацию о задании и отправителе

---

### 3. Просмотр деталей задания (Courier)

**Frontend**: `src/pages/TaskDetail.tsx`
- Курьер открывает детали задания
- Вызывает `api.getTask(taskId)`

**Backend**: `backend/src/routes/tasks.ts` → `GET /api/tasks/:id`
- Возвращает полную информацию о задании
- Проверяет что задание существует

**Логика отображения кнопки "Стать курьером":**
```typescript
const canAssign = !isOwner && !task.courierId && task.status === 'active' && api.isAuthenticated();
```
- ✅ Кнопка показывается только если:
  - Пользователь не является владельцем задания
  - Задание еще не имеет курьера (courier_id = NULL)
  - Задание имеет статус 'active'
  - Пользователь авторизован

---

### 4. Принятие задания (Courier)

**Frontend**: `src/pages/TaskDetail.tsx`
```typescript
const handleAssign = async () => {
  await api.assignTask(parseInt(id));
  toast.success("Вы назначены курьером!");
  navigate("/tasks");
};
```

**API**: `src/lib/api.ts`
```typescript
async assignTask(taskId: number) {
  return this.request(`/tasks/${taskId}/assign`, {
    method: 'POST',
  });
}
```

**Backend**: `backend/src/routes/tasks.ts` → `POST /api/tasks/:id/assign`
```typescript
router.post('/:id/assign', authenticateToken, async (req: AuthRequest, res) => {
  // 1. Проверка что задание существует
  // 2. Проверка что статус = 'active'
  // 3. Проверка что пользователь не является отправителем
  // 4. Обновление задания:
  //    - courier_id = req.userId
  //    - status = 'assigned'
  // 5. Отправка уведомлений курьеру и отправителю
  
  res.json({ success: true, message: 'Task assigned successfully' });
})
```

**Результат:**
- ✅ Задание обновлено: `courier_id = userId`, `status = 'assigned'`
- ✅ Задание больше не показывается в списке активных заданий
- ✅ Отправлены уведомления курьеру и отправителю

---

## ✅ Итоговая проверка логики

### Процесс создания задания:
1. ✅ Sender создает задание → статус `active`, `courier_id = NULL`
2. ✅ Задание появляется в списке для курьеров
3. ✅ Курьер видит задание в списке `/tasks`
4. ✅ Курьер открывает детали задания `/tasks/:id`
5. ✅ Курьер нажимает "Стать курьером"
6. ✅ Задание обновляется: `courier_id = userId`, `status = 'assigned'`
7. ✅ Задание больше не показывается в списке активных
8. ✅ Отправляются уведомления

### Проверка логики:

**✅ ВСЁ РЕАЛИЗОВАНО ПРАВИЛЬНО!**

1. ✅ Создание задания работает
2. ✅ Фильтрация заданий работает
3. ✅ Принятие задания работает
4. ✅ Проверки безопасности есть (нельзя назначить себя, нельзя назначить если уже есть курьер)
5. ✅ Статусы меняются правильно
6. ✅ Уведомления отправляются

---

## 🔧 Возможные улучшения (опционально)

1. **Улучшение UX:**
   - После принятия задания перенаправлять на страницу "Мои задания" вместо списка всех заданий
   - Показывать кнопку "Принять задание" прямо в списке заданий

2. **Улучшение логики:**
   - Добавить подтверждение перед принятием задания
   - Показывать информацию о курьере отправителю после принятия

3. **Улучшение фильтрации:**
   - Добавить фильтр по датам
   - Добавить фильтр по размеру посылки
   - Добавить фильтр по вознаграждению

---

## 📋 Вывод

**Логика создания и принятия заданий полностью реализована и работает правильно!**

- ✅ Все этапы работают
- ✅ Проверки безопасности есть
- ✅ Уведомления отправляются
- ✅ Статусы меняются правильно

**Статус**: ✅ **ВСЁ РАБОТАЕТ КОРРЕКТНО**

