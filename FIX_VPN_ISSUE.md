# 🔧 Решение проблемы: сайт не работает без VPN

Инструкция по диагностике и решению проблемы с доступом к сайту без VPN.

**Домен:** `sendbuddy.ru`  
**IP сервера:** `89.169.1.238`

---

## 🔍 Диагностика проблемы

### Шаг 1: Проверьте доступность IP

```bash
# Проверьте, доступен ли IP без VPN
ping 89.169.1.238

# Или через curl
curl -I http://89.169.1.238:3001/api/docs

# Если не работает - возможно IP заблокирован
```

### Шаг 2: Проверьте DNS

```bash
# Проверьте, резолвится ли домен
nslookup sendbuddy.ru
dig sendbuddy.ru

# Проверьте с разных DNS серверов
nslookup sendbuddy.ru 8.8.8.8  # Google DNS
nslookup sendbuddy.ru 1.1.1.1  # Cloudflare DNS
nslookup sendbuddy.ru 77.88.8.8  # Yandex DNS (Россия)
```

### Шаг 3: Проверьте SSL сертификат

```bash
# Проверьте SSL
openssl s_client -connect sendbuddy.ru:443 -servername sendbuddy.ru

# Или онлайн
# https://www.ssllabs.com/ssltest/analyze.html?d=sendbuddy.ru
```

---

## 🔧 Решения проблемы

### Решение 1: Проверьте DNS записи в Timeweb

**Проблема:** DNS записи могут быть неправильно настроены.

**Как проверить:**

1. Зайдите в панель Timeweb
2. Перейдите в **Домены** → `sendbuddy.ru`
3. Откройте **DNS-записи**
4. Убедитесь, что есть записи:

```
Тип: A
Имя: @
Значение: 89.169.1.238
TTL: 3600

Тип: A
Имя: www
Значение: 89.169.1.238
TTL: 3600
```

**Или:**

```
Тип: A
Имя: @
Значение: 89.169.1.238

Тип: CNAME
Имя: www
Значение: sendbuddy.ru
```

### Решение 2: Используйте Cloudflare (рекомендуется)

**Почему:** Cloudflare может помочь обойти блокировки и ускорить работу.

#### Шаг 1: Настройте Cloudflare

1. Зарегистрируйтесь на [cloudflare.com](https://cloudflare.com) (бесплатно)
2. Добавьте домен `sendbuddy.ru`
3. Cloudflare автоматически найдет DNS записи
4. Обновите Nameservers в Timeweb на те, что дал Cloudflare

#### Шаг 2: Настройте DNS в Cloudflare

Добавьте записи:

```
Тип: A
Имя: @
Значение: 89.169.1.238
Прокси: ✅ (оранжевое облако) - это важно!

Тип: A
Имя: www
Значение: 89.169.1.238
Прокси: ✅ (оранчевое облако)
```

#### Шаг 3: Настройте SSL в Cloudflare

1. В Cloudflare перейдите в **SSL/TLS**
2. Выберите режим: **Flexible** (если backend не на HTTPS) или **Full** (если на HTTPS)
3. Включите **Always Use HTTPS**

#### Шаг 4: Обновите Nginx на сервере

Если используете Cloudflare, обновите Nginx для работы с прокси:

```nginx
# В nginx.conf.production добавьте в server блок:

# Получаем реальный IP от Cloudflare
set_real_ip_from 103.21.244.0/22;
set_real_ip_from 103.22.200.0/22;
set_real_ip_from 103.31.4.0/22;
set_real_ip_from 104.16.0.0/13;
set_real_ip_from 104.24.0.0/14;
set_real_ip_from 108.162.192.0/18;
set_real_ip_from 131.0.72.0/22;
set_real_ip_from 141.101.64.0/18;
set_real_ip_from 162.158.0.0/15;
set_real_ip_from 172.64.0.0/13;
set_real_ip_from 173.245.48.0/20;
set_real_ip_from 188.114.96.0/20;
set_real_ip_from 190.93.240.0/20;
set_real_ip_from 197.234.240.0/22;
set_real_ip_from 198.41.128.0/17;
set_real_ip_from 2400:cb00::/32;
set_real_ip_from 2606:4700::/32;
set_real_ip_from 2803:f800::/32;
set_real_ip_from 2405:b500::/32;
set_real_ip_from 2405:8100::/32;
set_real_ip_from 2c0f:f248::/32;
set_real_ip_from 2a06:98c0::/29;
real_ip_header CF-Connecting-IP;
```

### Решение 3: Проверьте Firewall на сервере

**Проблема:** Firewall может блокировать подключения из России.

```bash
# Подключитесь к серверу
ssh root@89.169.1.238

# Проверьте firewall
ufw status

# Убедитесь, что открыты порты
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 22/tcp
ufw allow 3001/tcp  # для backend API

# Проверьте iptables
iptables -L -n
```

### Решение 4: Проверьте Nginx конфигурацию

```bash
# На сервере проверьте конфигурацию
nginx -t

# Проверьте, что Nginx слушает правильные порты
netstat -tlnp | grep nginx

# Должно быть:
# :80 (HTTP)
# :443 (HTTPS)
```

### Решение 5: Используйте CDN для статики

**Проблема:** Если IP заблокирован, можно использовать CDN для frontend.

**Варианты:**
- Cloudflare Pages (бесплатно)
- Vercel (бесплатно)
- Netlify (бесплатно)

Подключите домен к CDN, а backend оставьте на Timeweb.

---

## 🎯 Рекомендуемое решение: Cloudflare

**Почему Cloudflare:**
1. ✅ Бесплатно
2. ✅ Помогает обходить блокировки
3. ✅ Ускоряет сайт
4. ✅ Защита от DDoS
5. ✅ Автоматический SSL

**Пошаговая инструкция:**

### 1. Настройка Cloudflare

1. Зарегистрируйтесь на [cloudflare.com](https://cloudflare.com)
2. Добавьте домен `sendbuddy.ru`
3. Выберите бесплатный план
4. Cloudflare покажет Nameservers (например: `ns1.cloudflare.com`, `ns2.cloudflare.com`)

### 2. Обновите Nameservers в Timeweb

1. Зайдите в панель Timeweb
2. Домены → `sendbuddy.ru` → **DNS серверы** или **Nameservers**
3. Измените на значения от Cloudflare
4. Сохраните

### 3. Настройте DNS в Cloudflare

1. В Cloudflare перейдите в **DNS** → **Records**
2. Добавьте записи:

```
Type: A
Name: @
Content: 89.169.1.238
Proxy: ✅ (Proxied - оранжевое облако)

Type: A
Name: www
Content: 89.169.1.238
Proxy: ✅ (Proxied)
```

### 4. Настройте SSL в Cloudflare

1. **SSL/TLS** → **Overview**
2. Режим: **Flexible** (если backend HTTP) или **Full** (если HTTPS)
3. **Always Use HTTPS**: Включить
4. **Automatic HTTPS Rewrites**: Включить

### 5. Обновите Nginx на сервере

Добавьте поддержку Cloudflare IPs (см. код выше в "Решение 2")

### 6. Подождите распространения DNS

Обычно 5-30 минут, максимум 24 часа.

### 7. Проверьте

Откройте `https://sendbuddy.ru` без VPN - должно работать!

---

## 🔍 Дополнительная диагностика

### Проверка с разных локаций

Используйте онлайн инструменты:
- [downforeveryoneorjustme.com](https://www.downforeveryoneorjustme.com/sendbuddy.ru)
- [isitdownrightnow.com](https://www.isitdownrightnow.com/sendbuddy.ru)
- [dnschecker.org](https://dnschecker.org/#A/sendbuddy.ru)

### Проверка блокировки IP

```bash
# Проверьте, заблокирован ли IP в России
# https://reestr.rublacklist.net/
# Или через онлайн сервисы
```

### Логи на сервере

```bash
# Проверьте логи Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# Проверьте логи backend
docker logs sendbuddy-backend
# ИЛИ
pm2 logs sendbuddy-backend
```

---

## ✅ Чеклист проверки

- [ ] DNS записи настроены правильно
- [ ] Nameservers обновлены (если используете Cloudflare)
- [ ] SSL сертификат работает
- [ ] Nginx слушает порты 80 и 443
- [ ] Firewall не блокирует подключения
- [ ] Backend доступен на порту 3001
- [ ] Сайт работает через Cloudflare (если настроили)
- [ ] HTTPS редирект работает

---

## 🆘 Если ничего не помогает

### Альтернатива 1: Перенести на другой хостинг

Если IP заблокирован, можно:
- Попросить новый IP в Timeweb
- Перейти на другой VPS провайдер
- Использовать Cloudflare для проксирования

### Альтернатива 2: Использовать поддомен

Используйте поддомен, который не заблокирован:
- `app.sendbuddy.ru`
- `www.sendbuddy.ru`
- `send.sendbuddy.ru`

### Альтернатива 3: Frontend на CDN

Разместите frontend на Vercel/Netlify, а backend оставьте на Timeweb.

---

## 📞 Нужна помощь?

1. Проверьте логи на сервере
2. Проверьте DNS через онлайн инструменты
3. Обратитесь в поддержку Timeweb
4. Если используете Cloudflare - проверьте их статус

---

**Главное решение: используйте Cloudflare!** 🚀

Он решает большинство проблем с доступностью и блокировками.

