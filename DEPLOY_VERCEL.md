# 🚀 Деплой Frontend на Vercel

Пошаговая инструкция по развертыванию SendBuddy Frontend на Vercel.

## 📋 Вариант 1: Через Vercel CLI (рекомендуется)

### Шаг 1: Установите Vercel CLI

```bash
npm install -g vercel
```

### Шаг 2: Войдите в Vercel

```bash
vercel login
```

Откроется браузер для авторизации.

### Шаг 3: Деплой

```bash
cd /Users/ahmeddevops/Desktop/SendBuddynew

# Создайте .env.production (если еще не создан)
echo "VITE_API_URL=http://89.169.1.238:3001/api" > .env.production

# Соберите проект
npm run build

# Деплой
vercel --prod
```

Vercel автоматически:
- Определит настройки проекта
- Задеплоит папку `dist/`
- Даст вам URL (например: `https://sendbuddy.vercel.app`)

## 📋 Вариант 2: Через веб-интерфейс Vercel

### Шаг 1: Зарегистрируйтесь

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub (рекомендуется)

### Шаг 2: Импортируйте проект

1. Нажмите "Add New Project"
2. Выберите репозиторий `ahmed11551/parcel-pal-08`
3. Настройте проект:
   - **Framework Preset:** Vite
   - **Root Directory:** `./` (корень проекта)
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

### Шаг 3: Добавьте переменные окружения

В настройках проекта добавьте:
- **Key:** `VITE_API_URL`
- **Value:** `http://89.169.1.238:3001/api`

### Шаг 4: Деплой

Нажмите "Deploy" - Vercel автоматически соберет и задеплоит проект.

## 📋 Вариант 3: Ручная загрузка (быстрый способ)

### Шаг 1: Соберите проект локально

```bash
cd /Users/ahmeddevops/Desktop/SendBuddynew

# Создайте .env.production
echo "VITE_API_URL=http://89.169.1.238:3001/api" > .env.production

# Соберите
npm run build
```

### Шаг 2: Загрузите на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Выберите "Upload" (если доступно)
4. Перетащите папку `dist/`
5. Нажмите "Deploy"

## ⚙️ Настройка после деплоя

### Обновление API URL

После деплоя получите URL (например: `https://sendbuddy.vercel.app`)

Обновите `.env.production` для использования HTTPS:

```bash
echo "VITE_API_URL=https://api.yourdomain.com/api" > .env.production
```

Или если используете IP (пока без домена):

```bash
echo "VITE_API_URL=http://89.169.1.238:3001/api" > .env.production
```

### Настройка CORS на Backend

Убедитесь, что в backend `.env` указан правильный `FRONTEND_URL`:

```env
FRONTEND_URL=https://your-vercel-app.vercel.app
```

Или если используете IP:

```env
FRONTEND_URL=http://89.169.1.238
```

## 🔄 Обновление проекта

### Через CLI:

```bash
cd /Users/ahmeddevops/Desktop/SendBuddynew
npm run build
vercel --prod
```

### Через Git (автоматически):

Если подключили GitHub репозиторий, Vercel автоматически деплоит при каждом push в main ветку.

## ✅ Проверка

После деплоя:

1. Откройте URL вашего проекта (например: `https://sendbuddy.vercel.app`)
2. Проверьте, что frontend загружается
3. Попробуйте зарегистрироваться/войти
4. Проверьте консоль браузера на ошибки

## 🐛 Решение проблем

### Ошибка CORS

Если видите ошибки CORS, обновите `FRONTEND_URL` в backend `.env`:

```bash
# На сервере
cd ~/parcel-pal-08/backend
nano .env
# Измените FRONTEND_URL на ваш Vercel URL
docker restart sendbuddy-backend
```

### API не доступен

Проверьте:
1. Backend работает: `http://89.169.1.238:3001/api/docs`
2. Firewall открыт для порта 3001
3. `VITE_API_URL` правильный в `.env.production`

## 🎉 Готово!

Ваш frontend задеплоен на Vercel!

**Следующие шаги:**
1. Настройте домен (опционально)
2. Настройте SSL для backend (Let's Encrypt)
3. Протестируйте все функции

---

**Нужна помощь?** Проверьте логи в Vercel Dashboard или консоль браузера.

