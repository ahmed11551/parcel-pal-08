# 🚀 Первые шаги после покупки сервера

## 1️⃣ Обновите DNS записи (если нужно)

Ваш домен уже добавлен! Нужно только обновить A-записи на новый IP.

**IP адрес сервера:** 194.67.124.90

См. [DNS_UPDATE_GUIDE.md](DNS_UPDATE_GUIDE.md) - как обновить DNS записи.

## 2️⃣ Подключитесь к серверу

### С Mac/Linux:

```bash
ssh root@194.67.124.90
```

При запросе введите пароль: `n3Ij63UBc4osWD6m`

**IP адрес сервера:** 194.67.124.90 (плавающий IP)

### С Windows:

Используйте:
- **PuTTY** - [скачать](https://www.putty.org/)
- **Windows Terminal** (встроен в Windows 10/11)
- **WSL** (Windows Subsystem for Linux)

## 3️⃣ Первая настройка (обязательно!)

После подключения выполните:

```bash
# Обновление системы
apt update && apt upgrade -y
```

⚠️ **Если появится диалог об обновлении ядра:**
- Нажмите `<Ok>` - можно продолжить без перезагрузки
- Перезагрузите сервер позже: `reboot`
- См. [KERNEL_UPGRADE.md](KERNEL_UPGRADE.md) для подробностей

⚠️ **Если появится диалог о перезапуске сервисов:**
- Оставьте выбранные по умолчанию
- Нажмите `<Ok>` - это безопасно и быстро
- См. [SERVICES_RESTART.md](SERVICES_RESTART.md) для подробностей

# Смена пароля (рекомендуется)
passwd
# Введите новый надежный пароль

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# Проверка
docker --version
docker compose version
```

## 4️⃣ Настройка firewall

```bash
apt install ufw -y
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw enable
ufw status
```

## 5️⃣ Клонирование проекта

```bash
# Установка Git (если нет)
apt install git -y

# Клонирование
git clone https://github.com/ваш-username/SendBuddynew.git
cd SendBuddynew
```

## 6️⃣ Оптимизация для 2GB RAM

```bash
chmod +x optimize-2gb-ram.sh
./optimize-2gb-ram.sh
```

## 7️⃣ Настройка переменных окружения

```bash
cp .env.production.example .env.production
nano .env.production
```

Заполните:
- `DB_PASSWORD` - сгенерируйте: `openssl rand -base64 24`
- `JWT_SECRET` - сгенерируйте: `openssl rand -hex 32`
- `FRONTEND_URL` - ваш IP или домен (пока можно оставить IP)
- `VITE_API_URL` - ваш IP/api или домен/api

## 8️⃣ Деплой

```bash
chmod +x deploy.sh
./deploy.sh
```

## 9️⃣ Проверка

```bash
# Статус контейнеров
docker compose ps

# Логи
docker compose logs -f

# Health check
curl http://localhost:3001/api/health
```

## ✅ Готово!

Ваш проект должен быть доступен по адресам:
- Frontend: `https://send-buddy.ru`
- Backend API: `https://send-buddy.ru/api`
- IP адрес сервера: `194.67.124.90` (плавающий IP)
- Приватный IP: `192.168.0.107` (внутренняя сеть)

## 🌐 Настройка домена send-buddy.ru

У вас уже настроен домен send-buddy.ru! 

**После деплоя выполните:**

```bash
# Автоматическая настройка Nginx и SSL
chmod +x setup-domain.sh
./setup-domain.sh
```

Скрипт автоматически:
- ✅ Настроит Nginx как reverse proxy
- ✅ Установит SSL сертификат (Let's Encrypt)
- ✅ Обновит переменные окружения
- ✅ Перезапустит контейнеры

**Или** следуйте инструкциям в [DOMAIN_SETUP.md](DOMAIN_SETUP.md) для ручной настройки.

## ⚠️ Важно

- **Сохраните пароли в безопасном месте**
- **Смените пароль root** на более надежный
- **Настройте SSH ключи** для безопасности
- **Не коммитьте пароли в Git!**

## 📚 Дальнейшие шаги

- [REG_RU_QUICK_START.md](REG_RU_QUICK_START.md) - быстрый старт
- [DOCKER_COMPOSE_GUIDE.md](DOCKER_COMPOSE_GUIDE.md) - детальная инструкция
- [YOUR_SERVER_CONFIG.md](YOUR_SERVER_CONFIG.md) - оптимизация для 2GB RAM

