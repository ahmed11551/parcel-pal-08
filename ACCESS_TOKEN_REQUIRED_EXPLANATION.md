# Объяснение ошибки "Access token required"

## 🔍 Что это значит?

**"Access token required"** (Токен доступа требуется) - это ошибка, которая возникает когда:

1. Пользователь пытается получить доступ к защищенному API endpoint
2. Токен авторизации (JWT) не был передан в запросе
3. Или токен не найден в localStorage

## 📋 Когда это возникает?

### В SendBuddy это происходит когда:

- Пользователь не авторизован, но пытается:
  - Создать задание (`POST /api/tasks`)
  - Посмотреть свои задания
  - Изменить профиль
  - Выполнить другие действия, требующие авторизации

### Защищенные endpoints:

- `POST /api/tasks` - создание задания
- `PUT /api/tasks/:id` - обновление задания
- `POST /api/tasks/:id/assign` - принятие задания
- `GET /api/users/me` - получение профиля
- И другие...

## ✅ Как это исправить?

### 1. Убедитесь что пользователь авторизован

Пользователь должен:
- Зарегистрироваться через SMS или Telegram
- Или войти через `/login`

### 2. Проверьте что токен сохраняется

После авторизации токен должен сохраняться в localStorage:
```javascript
// Проверьте в консоли браузера:
localStorage.getItem('token')
```

### 3. Проверьте что токен передается в запросах

Токен должен автоматически добавляться в заголовки:
```javascript
Authorization: Bearer <token>
```

## 🔧 Технические детали

### Backend проверяет токен:

```typescript
// backend/src/middleware/auth.ts
export function authenticateToken(req, res, next) {
  const authHeader = req.headers['authorization'];
  const token = authHeader && authHeader.split(' ')[1]; // Bearer TOKEN

  if (!token) {
    return res.status(401).json({ error: 'Access token required' });
  }
  // ... проверка токена
}
```

### Frontend должен отправлять токен:

```typescript
// src/lib/api.ts
private async request<T>(endpoint: string, options: RequestInit = {}) {
  const token = this.getToken(); // Получаем токен из localStorage
  
  const headers: HeadersInit = {
    'Content-Type': 'application/json',
    ...options.headers,
  };

  if (token) {
    headers['Authorization'] = `Bearer ${token}`; // Добавляем токен
  }
  // ... отправка запроса
}
```

## 🐛 Возможные проблемы и решения

### Проблема 1: Токен не сохраняется после авторизации

**Решение:**
- Проверьте что после `/register/verify` или `/login/verify` токен сохраняется
- Проверьте консоль браузера на ошибки

### Проблема 2: Токен истек

**Решение:**
- Токен действителен 7 дней
- Если истек, нужно войти заново

### Проблема 3: Токен не передается в запросах

**Решение:**
- Проверьте что `getToken()` возвращает токен
- Проверьте Network tab в DevTools - должен быть заголовок `Authorization`

## 📊 Как проверить в браузере

### 1. Откройте DevTools (F12)

### 2. Перейдите в Application → Local Storage

### 3. Проверьте наличие ключа `token`

### 4. Перейдите в Network tab

### 5. Выполните действие, которое требует авторизации

### 6. Проверьте запрос:
- Должен быть заголовок `Authorization: Bearer <token>`
- Если заголовка нет - проблема в передаче токена

## ✅ Правильный поток авторизации

1. Пользователь регистрируется/входит
2. Backend возвращает JWT токен
3. Frontend сохраняет токен в localStorage
4. При каждом запросе токен добавляется в заголовок `Authorization`
5. Backend проверяет токен и разрешает доступ

## 🎯 Вывод

**"Access token required"** = Пользователь не авторизован или токен не передан.

**Решение:** Авторизоваться через `/register` или `/login`, или через Telegram бота.

