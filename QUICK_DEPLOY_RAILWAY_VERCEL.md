# 🚀 Быстрый деплой на Railway + Vercel

Пошаговая инструкция для деплоя SendBuddy за 15-20 минут.

---

## 📋 Подготовка

### ✅ Что нужно:
- [ ] GitHub репозиторий с кодом проекта
- [ ] Аккаунт на [railway.app](https://railway.app) (бесплатно)
- [ ] Аккаунт на [vercel.com](https://vercel.com) (бесплатно)
- [ ] Email для регистрации

---

## 🎯 Шаг 1: Деплой Backend на Railway

### 1.1. Зарегистрируйтесь на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Выберите **"Login with GitHub"** (рекомендуется)
4. Авторизуйтесь через GitHub

### 1.2. Создайте новый проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий (`parcel-pal-08` или другой)
4. Railway автоматически определит проект

### 1.3. Настройте Backend сервис

1. Railway откроет ваш проект
2. Нажмите **"Add Service"** → **"GitHub Repo"**
3. Выберите тот же репозиторий
4. В настройках проекта:
   - **Root Directory:** укажите `backend`
   - Railway автоматически определит NestJS

### 1.4. Добавьте PostgreSQL базу данных

1. В том же проекте нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст PostgreSQL
3. **Важно:** Запишите переменные окружения, которые Railway создаст:
   - `PGHOST`
   - `PGPORT`
   - `PGDATABASE`
   - `PGUSER`
   - `PGPASSWORD`
   - Или используйте `DATABASE_URL` (объединяет все)

### 1.5. Настройте переменные окружения

1. В настройках Backend сервиса откройте **"Variables"** (или **"Environment"**)
2. Нажмите **"New Variable"** и добавьте следующие переменные:

```env
# Приложение
NODE_ENV=production
PORT=3001

# База данных (Railway автоматически создаст, но можно использовать DATABASE_URL)
# Используйте переменные из PostgreSQL сервиса:
# PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD
# ИЛИ одну переменную DATABASE_URL (если Railway её создаст)

# Если Railway не создал DATABASE_URL, создайте вручную:
# Формат: postgresql://user:password@host:port/database

# Для Railway можно использовать переменные окружения из PostgreSQL сервиса:
# DB_HOST=${{Postgres.PGHOST}}
# DB_PORT=${{Postgres.PGPORT}}
# DB_USERNAME=${{Postgres.PGUSER}}
# DB_PASSWORD=${{Postgres.PGPASSWORD}}
# DB_DATABASE=${{Postgres.PGDATABASE}}

# Или проще - используйте DATABASE_URL если Railway его создал:
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT (создайте случайную строку минимум 32 символа)
JWT_SECRET=sendbuddy-secret-key-change-in-production-min-32-characters-long-railway-2024
JWT_EXPIRES_IN=7d

# Frontend URL (пока оставьте пустым, заполним после деплоя Vercel)
FRONTEND_URL=https://your-frontend.vercel.app

# Остальные переменные (можно настроить позже или оставить пустыми)
# SMS (Twilio) - опционально
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Платежи (ЮKassa) - опционально
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
PAYMENT_RETURN_URL=

# S3 Storage - опционально
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=
AWS_S3_ENDPOINT=

# Throttling
THROTTLE_TTL=60
THROTTLE_LIMIT=100
```

**Важно:** Railway автоматически создаст переменные для PostgreSQL. Вы можете использовать их через синтаксис `${{Service.Variable}}` или просто добавить их вручную.

### 1.6. Настройте деплой

1. В настройках сервиса откройте **"Settings"**
2. Убедитесь, что:
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm run start:prod`
   - **Root Directory:** `backend`

3. Нажмите **"Deploy"** или Railway автоматически задеплоит при изменении кода

### 1.7. Получите URL Backend

1. После деплоя Railway даст вам URL (например: `https://your-backend.up.railway.app`)
2. **Запишите этот URL!** Он понадобится для Frontend
3. Проверьте работу: откройте `https://your-backend.up.railway.app/api/docs`

---

## 🎨 Шаг 2: Деплой Frontend на Vercel

### 2.1. Зарегистрируйтесь на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"** (рекомендуется)
4. Авторизуйтесь через GitHub

### 2.2. Импортируйте проект

1. Нажмите **"Add New Project"**
2. Найдите и выберите ваш репозиторий (`parcel-pal-08` или другой)
3. Нажмите **"Import"**

### 2.3. Настройте проект

Vercel автоматически определит Vite, но убедитесь в настройках:

1. **Framework Preset:** Vite
2. **Root Directory:** `./` (корень проекта)
3. **Build Command:** `npm run build`
4. **Output Directory:** `dist`
5. **Install Command:** `npm install`

### 2.4. Добавьте переменные окружения

1. В разделе **"Environment Variables"** добавьте:

```
VITE_API_URL=https://your-backend.up.railway.app/api
```

**Замените `your-backend.up.railway.app` на ваш реальный Railway URL!**

### 2.5. Обновите vercel.json (опционально)

Vercel может автоматически определить конфигурацию, но если нужно, обновите `vercel.json`:

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### 2.6. Задеплойте

1. Нажмите **"Deploy"**
2. Vercel начнет сборку (это займет 2-3 минуты)
3. После завершения вы получите URL (например: `https://your-app.vercel.app`)

---

## 🔄 Шаг 3: Обновите Backend URL в Frontend

### 3.1. Обновите переменную окружения в Vercel

1. Зайдите в настройки проекта на Vercel
2. Откройте **"Environment Variables"**
3. Обновите `VITE_API_URL` на ваш реальный Railway URL
4. Нажмите **"Redeploy"**

### 3.2. Обновите FRONTEND_URL в Railway

1. Зайдите в настройки Backend сервиса на Railway
2. Откройте **"Variables"**
3. Обновите `FRONTEND_URL` на ваш Vercel URL:
   ```
   FRONTEND_URL=https://your-app.vercel.app
   ```
4. Railway автоматически перезапустит сервис

---

## ✅ Шаг 4: Проверка работы

### 4.1. Проверьте Backend

Откройте в браузере:
- **API Docs:** `https://your-backend.up.railway.app/api/docs`
- Должна открыться Swagger документация

### 4.2. Проверьте Frontend

Откройте в браузере:
- **Frontend:** `https://your-app.vercel.app`
- Должен открыться сайт

### 4.3. Проверьте интеграцию

1. Откройте Frontend
2. Откройте консоль браузера (F12)
3. Попробуйте зарегистрироваться
4. Проверьте, что запросы идут на правильный API URL

---

## 🔧 Шаг 5: Настройка базы данных (важно!)

### 5.1. Проверьте подключение к БД

Railway автоматически создал PostgreSQL, но нужно убедиться, что backend подключается правильно.

### 5.2. Если есть ошибки подключения

Проверьте, что в переменных окружения правильно указаны данные БД:

1. В Railway откройте PostgreSQL сервис
2. Скопируйте переменные:
   - `PGHOST`
   - `PGPORT`
   - `PGDATABASE`
   - `PGUSER`
   - `PGPASSWORD`

3. В Backend сервисе добавьте их:
   ```
   DB_HOST=${{Postgres.PGHOST}}
   DB_PORT=${{Postgres.PGPORT}}
   DB_DATABASE=${{Postgres.PGDATABASE}}
   DB_USERNAME=${{Postgres.PGUSER}}
   DB_PASSWORD=${{Postgres.PGPASSWORD}}
   ```

### 5.3. Проверьте логи

В Railway откройте Backend сервис → **"Deployments"** → **"View Logs"**

Если видите ошибки подключения к БД - проверьте переменные окружения.

---

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте логи в Railway
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте, что PostgreSQL сервис запущен

### Frontend не подключается к API

1. Проверьте `VITE_API_URL` в Vercel
2. Проверьте CORS настройки в backend (должен быть `FRONTEND_URL`)
3. Проверьте логи в Railway

### Ошибка CORS

1. Убедитесь, что `FRONTEND_URL` в Railway указывает на ваш Vercel домен
2. Перезапустите Backend сервис в Railway

### База данных не подключается

1. Проверьте, что PostgreSQL сервис запущен в Railway
2. Проверьте переменные окружения (DB_HOST, DB_PORT, и т.д.)
3. Убедитесь, что Backend и PostgreSQL в одном проекте Railway

---

## 📝 Чеклист

- [ ] Создан аккаунт на Railway
- [ ] Создан аккаунт на Vercel
- [ ] Backend задеплоен на Railway
- [ ] PostgreSQL создан в Railway
- [ ] Все переменные окружения добавлены в Railway
- [ ] Frontend задеплоен на Vercel
- [ ] `VITE_API_URL` настроен в Vercel
- [ ] `FRONTEND_URL` настроен в Railway
- [ ] API работает: `/api/docs` открывается
- [ ] Frontend работает: сайт открывается
- [ ] Интеграция работает: можно зарегистрироваться

---

## 🎉 Готово!

Ваш проект развернут:
- **Backend:** `https://your-backend.up.railway.app`
- **Frontend:** `https://your-app.vercel.app`
- **API Docs:** `https://your-backend.up.railway.app/api/docs`

---

## 🔄 Обновление проекта

### Backend

Просто сделайте `git push` - Railway автоматически задеплоит изменения!

### Frontend

Просто сделайте `git push` - Vercel автоматически задеплоит изменения!

---

## 💰 Стоимость

**Бесплатно для тестирования!**

- Railway: 500 часов/месяц бесплатно (достаточно для тестирования)
- Vercel: полностью бесплатно для личных проектов
- PostgreSQL на Railway: бесплатно (с ограничениями)

**Когда нужен платный план:**
- Railway: если проект работает 24/7 (после 500 часов)
- Vercel: для коммерческих проектов с большим трафиком

---

## 📚 Полезные ссылки

- [Railway Documentation](https://docs.railway.app/)
- [Vercel Documentation](https://vercel.com/docs)
- [Railway Discord](https://discord.gg/railway)
- [Vercel Discord](https://vercel.com/discord)

---

**Удачи с деплоем!** 🚀

