# 🚀 Деплой на Timeweb Cloud - Прямо сейчас!

Пошаговая инструкция для развертывания SendBuddy на Timeweb Cloud.

## ⚡ Быстрый план (30-40 минут)

### Этап 1: Создание инфраструктуры в Timeweb Cloud (10 мин)

1. **Зайдите на [timeweb.cloud](https://timeweb.cloud/)**
2. **Создайте VPS для Backend:**
   - "Облачные серверы" → "Создать сервер"
   - Ubuntu 22.04 LTS
   - 2 CPU, 4 GB RAM, 40 GB SSD
   - Регион: Москва
   - **Запишите IP адрес!**

3. **Создайте PostgreSQL:**
   - "Облачные базы данных" → "Создать базу"
   - PostgreSQL 16
   - 2 CPU, 4 GB RAM, 20 GB SSD
   - **Запишите:** хост, порт, пользователь, пароль

4. **Создайте Redis:**
   - "Облачные базы данных" → "Создать базу"
   - Redis 7
   - 1 CPU, 2 GB RAM, 10 GB SSD
   - **Запишите:** хост, порт, пароль

5. **Создайте S3 Bucket:**
   - "Объектное хранилище S3" → "Создать бакет"
   - Название: `sendbuddy-files`
   - **Создайте ключи доступа** и запишите их

### Этап 2: Подключение к серверу (2 мин)

```bash
# Скопируйте IP адрес вашего VPS и выполните:
ssh root@YOUR_SERVER_IP

# Или используйте пароль из панели управления
```

### Этап 3: Автоматическая установка (5 мин)

**Вариант A: Автоматический скрипт (рекомендуется)**

1. Скопируйте файл `install-on-server.sh` на сервер (через SFTP или создайте на сервере)
2. Выполните на сервере:

```bash
# Скачайте скрипт
wget https://raw.githubusercontent.com/ahmed11551/parcel-pal-08/main/install-on-server.sh
# Или создайте вручную и скопируйте содержимое

# Запустите
bash install-on-server.sh
```

**Вариант B: Ручная установка**

```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
apt install docker-compose-plugin -y

# Устанавливаем Git
apt install git -y

# Клонируем проект
git clone https://github.com/ahmed11551/parcel-pal-08.git
cd parcel-pal-08/backend
```

### Этап 4: Настройка .env (5 мин)

```bash
# Создаем .env файл
nano .env
```

**Скопируйте этот шаблон и заполните своими данными:**

```env
NODE_ENV=production
PORT=3001

# PostgreSQL (из Timeweb Cloud)
DB_TYPE=postgres
DB_HOST=ВАШ_ХОСТ_БД.timeweb.cloud
DB_PORT=5432
DB_USERNAME=sendbuddy
DB_PASSWORD=ВАШ_ПАРОЛЬ_БД
DB_DATABASE=sendbuddy

# Redis (из Timeweb Cloud)
REDIS_HOST=ВАШ_ХОСТ_REDIS.timeweb.cloud
REDIS_PORT=6379
REDIS_PASSWORD=ВАШ_ПАРОЛЬ_REDIS

# JWT (создайте случайную строку минимум 32 символа)
JWT_SECRET=$(openssl rand -base64 32)
JWT_EXPIRES_IN=7d

# Frontend URL (пока можно оставить IP или домен)
FRONTEND_URL=http://YOUR_SERVER_IP

# S3 Storage (Timeweb Cloud)
AWS_ACCESS_KEY_ID=ВАШ_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=ВАШ_SECRET_ACCESS_KEY
AWS_REGION=ru-1
AWS_S3_BUCKET=sendbuddy-files
AWS_S3_ENDPOINT=https://s3.timeweb.cloud

# SMS (Twilio - можно настроить позже)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Платежи (ЮKassa - можно настроить позже)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# Throttling
THROTTLE_TTL=60
THROTTLE_LIMIT=100
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

### Этап 5: Запуск Backend (3 мин)

```bash
# Собираем Docker образ
docker build -t sendbuddy-backend .

# Запускаем контейнер
docker run -d \
  --name sendbuddy-backend \
  --restart unless-stopped \
  -p 3001:3001 \
  --env-file .env \
  sendbuddy-backend

# Проверяем логи
docker logs -f sendbuddy-backend
```

**Ожидаемый результат:**
```
Application is running on: http://localhost:3001
Swagger docs available at: http://localhost:3001/api/docs
```

**Если видите ошибки:**
- Проверьте подключение к БД (логи покажут проблему)
- Убедитесь, что IP сервера добавлен в whitelist БД в панели Timeweb Cloud

### Этап 6: Настройка Firewall (2 мин)

В панели Timeweb Cloud:
1. Откройте ваш VPS
2. "Сеть" → "Firewall"
3. Разрешите порты:
   - **22** (SSH)
   - **3001** (Backend API)
   - **80** (HTTP, если будете ставить Nginx)
   - **443** (HTTPS, если будете ставить SSL)

### Этап 7: Проверка работы (2 мин)

```bash
# Проверьте, что контейнер работает
docker ps

# Проверьте логи
docker logs sendbuddy-backend

# Проверьте API (с другого терминала или браузера)
curl http://YOUR_SERVER_IP:3001/api/docs
```

### Этап 8: Деплой Frontend (10 мин)

**Вариант A: На том же сервере (проще)**

```bash
# Выйдите из папки backend
cd ..

# Создайте .env.production
echo "VITE_API_URL=http://YOUR_SERVER_IP:3001/api" > .env.production

# Соберите frontend
docker build -t sendbuddy-frontend .

# Запустите
docker run -d \
  --name sendbuddy-frontend \
  --restart unless-stopped \
  -p 80:80 \
  sendbuddy-frontend
```

**Вариант B: На статическом хостинге (рекомендуется)**

1. **Локально на вашем компьютере:**

```bash
cd /Users/ahmeddevops/Desktop/SendBuddynew

# Создайте .env.production
echo "VITE_API_URL=http://YOUR_SERVER_IP:3001/api" > .env.production

# Соберите
npm run build
```

2. **Загрузите на Netlify:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Перетащите папку `dist/` в Netlify
   - Получите бесплатный домен

3. **Или загрузите на Vercel:**
   ```bash
   npm install -g vercel
   vercel --prod
   ```

## ✅ Проверка

1. **Backend API:** `http://YOUR_SERVER_IP:3001/api/docs`
2. **Frontend:** `http://YOUR_SERVER_IP` или ваш Netlify/Vercel домен

## 🔧 Если что-то пошло не так

### Backend не запускается

```bash
# Проверьте логи
docker logs sendbuddy-backend

# Проверьте .env файл
cat .env

# Перезапустите контейнер
docker restart sendbuddy-backend
```

### Ошибка подключения к БД

1. В панели Timeweb Cloud откройте вашу БД
2. "Доступ" → добавьте IP адрес вашего VPS в whitelist
3. Проверьте, что пароль правильный в .env

### Frontend не подключается к API

1. Проверьте `VITE_API_URL` в `.env.production`
2. Убедитесь, что backend доступен по указанному адресу
3. Проверьте CORS в backend (должен быть настроен)

## 📝 Чеклист

- [ ] Создан VPS в Timeweb Cloud
- [ ] Создана PostgreSQL база данных
- [ ] Создан Redis
- [ ] Создан S3 bucket
- [ ] Подключился к серверу по SSH
- [ ] Установлен Docker
- [ ] Клонирован проект
- [ ] Настроен .env файл
- [ ] Запущен backend контейнер
- [ ] Проверена работа API
- [ ] Задеплоен frontend
- [ ] Все работает!

## 🎉 Готово!

Ваш проект развернут на Timeweb Cloud!

**Следующие шаги:**
1. Настройте домен (опционально)
2. Установите SSL сертификат (Let's Encrypt)
3. Настройте SMS (Twilio)
4. Настройте платежи (ЮKassa)

---

**Нужна помощь?** Проверьте логи: `docker logs sendbuddy-backend`

