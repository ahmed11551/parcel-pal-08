# 🔄 Обновление на сервере - Правильные команды

## ✅ Правильные команды для вашего сервера

Проект находится в директории `/root/parcel-pal-08`, а не `/root/SendBuddynew`.

### Выполните эти команды на сервере:

```bash
# 1. Перейдите в правильную директорию проекта
cd /root/parcel-pal-08

# 2. Проверьте статус контейнеров
docker compose ps

# 3. Получите последние изменения из GitHub
git pull origin main
# или если ветка называется по-другому:
git pull

# 4. Пересоберите и перезапустите контейнеры
docker compose down
docker compose build --no-cache backend telegram-bot
docker compose up -d

# 5. Проверьте логи
docker compose logs -f backend telegram-bot
```

## 🔍 Если директории нет или проект не настроен

Если директория `/root/parcel-pal-08` не существует, нужно либо:
1. Клонировать проект заново
2. Или найти, где проект был установлен

### Поиск проекта:

```bash
# Поиск директорий с проектом
find /root -name "docker-compose.yml" -o -name "package.json" 2>/dev/null

# Поиск директорий с названием проекта
ls -la /root | grep -i parcel\|sendbuddy

# Проверка, где запущены Docker контейнеры
docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}"
```

## 📝 Альтернативный вариант (если используете другой путь)

Если проект находится в другой директории, замените путь:

```bash
cd /root/ваша-директория
# затем выполните те же команды
```

