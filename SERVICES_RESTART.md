# 🔄 Перезапуск сервисов после обновления

## ✅ Что это значит

После выполнения `apt upgrade` система обнаружила, что некоторые сервисы используют устаревшие библиотеки. Их нужно перезапустить, чтобы они начали использовать новые версии.

## 🤔 Что делать

### Рекомендуется: Оставить выбранные по умолчанию

**Система уже выбрала важные сервисы:**
- ✅ `cron.service` - планировщик задач
- ✅ `fail2ban.service` - защита от брутфорса
- ✅ `irqbalance.service` - балансировка прерываний
- ✅ `multipathd.service` - управление путями
- ✅ `polkit.service` - политики доступа
- ✅ `serial-getty@ttyS0.service` - консольный доступ

### Вариант 1: Оставить как есть (рекомендуется)

1. **Нажмите `<Ok>`** - перезапустятся только выбранные сервисы
2. Это безопасно и быстро
3. Продолжите настройку сервера

### Вариант 2: Выбрать все сервисы

Если хотите перезапустить все сервисы:

1. Используйте стрелки для навигации
2. Нажмите пробел для выбора/снятия выбора
3. Выберите все нужные сервисы
4. Нажмите `<Ok>`

### Вариант 3: Отменить

Если хотите перезапустить вручную позже:
- Нажмите `<Cancel>`
- Позже выполните: `systemctl restart <service-name>`

## ⚠️ Важно

- **Это безопасно** - перезапуск сервисов не перезагрузит весь сервер
- **SSH соединение не прервется** - только отдельные сервисы перезапустятся
- **Можно продолжить работу** сразу после перезапуска

## 📋 Что происходит при перезапуске

- Сервисы останавливаются на секунду
- Запускаются заново с новыми библиотеками
- Продолжают работать нормально

## ✅ Рекомендация

**Оставьте выбранные по умолчанию и нажмите `<Ok>`**

Это самый безопасный и быстрый вариант. После этого продолжите настройку сервера (установка Docker и т.д.).

## 🔍 Проверка после перезапуска

После нажатия `<Ok>` сервисы перезапустятся автоматически. Проверить можно:

```bash
systemctl status cron.service
systemctl status fail2ban.service
```

Все должно работать нормально!

