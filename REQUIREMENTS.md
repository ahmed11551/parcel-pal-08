# 📋 Что нужно для деплоя SendBuddy

## 🖥️ Требования к серверу

### Минимальные требования:
- **CPU**: 2 ядра
- **RAM**: 2GB (рекомендуется 4GB)
- **Диск**: 20GB SSD
- **ОС**: Ubuntu 20.04+ или Debian 11+

### Программное обеспечение:
- Docker 20.10+
- Docker Compose 2.0+
- Git
- Nginx (для reverse proxy)
- Certbot (для SSL сертификатов)

## 🌐 Доменное имя (опционально, но рекомендуется)

- Зарегистрированный домен (например: sendbuddy.ru)
- Доступ к DNS настройкам домена
- Возможность добавить A-запись, указывающую на IP сервера

## 💰 Ресурсы Reg.ru Cloud

1. **Облачный сервер (VPS)**
   - Минимум: 2 CPU, 2GB RAM, 20GB SSD
   - Стоимость: от ~300₽/месяц
   - Рекомендуется: 4 CPU, 4GB RAM, 40GB SSD

2. **Доменное имя** (опционально)
   - Стоимость: от ~200₽/год

## 🔑 Что нужно подготовить

### 1. Доступ к серверу
- IP адрес сервера
- SSH доступ (логин и пароль или SSH ключ)
- Root доступ или пользователь с sudo правами

### 2. Переменные окружения

Создайте файл `.env.production` со следующими переменными:

```env
# Database
DB_PASSWORD=сгенерируйте_надежный_пароль_минимум_16_символов

# JWT Secret (обязательно измените!)
JWT_SECRET=сгенерируйте_случайную_строку_минимум_32_символа
JWT_EXPIRES_IN=7d

# URLs
FRONTEND_URL=https://ваш-домен.ru
VITE_API_URL=https://ваш-домен.ru/api

# Upload settings
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880
```

**Как сгенерировать пароли:**
```bash
# DB_PASSWORD
openssl rand -base64 24

# JWT_SECRET
openssl rand -hex 32
```

### 3. Git репозиторий
- Код должен быть в Git репозитории (GitHub, GitLab и т.д.)
- Доступ к репозиторию с сервера (SSH ключ или HTTPS)

## 📝 Чеклист перед деплоем

### Подготовка:
- [ ] Сервер создан и доступен по SSH
- [ ] Доменное имя зарегистрировано (если используется)
- [ ] DNS записи настроены (A-запись на IP сервера)
- [ ] Переменные окружения подготовлены
- [ ] Пароли сгенерированы и сохранены в безопасном месте

### На сервере:
- [ ] Docker установлен
- [ ] Docker Compose установлен
- [ ] Git установлен
- [ ] Firewall настроен (порты 22, 80, 443 открыты)
- [ ] Nginx установлен (для reverse proxy)
- [ ] Certbot установлен (для SSL)

### Код:
- [ ] Код залит в Git репозиторий
- [ ] Все зависимости указаны в package.json
- [ ] .env.production.example создан

## 🚀 Быстрый старт

1. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш-ip-адрес
   ```

2. **Установите Docker и Docker Compose:**
   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh && sh get-docker.sh
   curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
   chmod +x /usr/local/bin/docker-compose
   ```

3. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ваш-username/SendBuddynew.git
   cd SendBuddynew
   ```

4. **Настройте переменные окружения:**
   ```bash
   cp .env.production.example .env.production
   nano .env.production
   ```

5. **Запустите деплой:**
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   ```

6. **Настройте Nginx и SSL** (см. [DOCKER_COMPOSE_GUIDE.md](DOCKER_COMPOSE_GUIDE.md))

## 📚 Документация

- 🚀 [Быстрый старт (10 минут)](REG_RU_QUICK_START.md)
- 📖 [Полное руководство](DOCKER_COMPOSE_GUIDE.md)
- 🛠️ [Локальная разработка](SETUP.md)

## ⚠️ Важно

1. **Безопасность:**
   - Никогда не коммитьте `.env.production` в Git
   - Используйте сильные пароли
   - Регулярно обновляйте систему
   - Настройте firewall

2. **Резервное копирование:**
   - Настройте автоматические бэкапы БД
   - Храните бэкапы в безопасном месте

3. **Мониторинг:**
   - Регулярно проверяйте логи
   - Настройте мониторинг ресурсов сервера

## 💡 Полезные ссылки

- [Reg.ru Cloud](https://www.reg.ru/cloud/)
- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)

