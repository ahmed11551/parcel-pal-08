# 🌐 Информация о сети сервера

## 📋 Текущая конфигурация

### Публичный доступ:
- **Плавающий IP**: 194.67.124.90
- **Используется для**: SSH, HTTP, HTTPS, внешний доступ

### Приватная сеть:
- **Сеть**: private_network_115049195
- **Подсеть**: subnet_115049195
- **Приватный IP**: 192.168.0.107
- **Используется для**: внутренняя коммуникация между серверами

## ⚠️ Важная информация

### Заблокированные порты:
- **Порт 25** (SMTP) - заблокирован
- **Порт 465** (SMTPS) - заблокирован

**Это нормально!** Эти порты используются для отправки email. Для разблокировки нужно обратиться в поддержку Reg.ru, но для SendBuddy они не нужны (мы используем внешний SMS сервис).

### Открытые порты (нужно открыть):
- **Порт 22** (SSH) - для подключения
- **Порт 80** (HTTP) - для веб-доступа
- **Порт 443** (HTTPS) - для защищенного веб-доступа
- **Порт 3000** (Frontend) - временно, потом через Nginx
- **Порт 3001** (Backend) - временно, потом через Nginx

## 🔧 Настройка firewall

После подключения к серверу выполните:

```bash
ufw allow 22/tcp    # SSH
ufw allow 80/tcp   # HTTP
ufw allow 443/tcp   # HTTPS
ufw enable
```

**Не открывайте порты 3000 и 3001 наружу** - они будут доступны только через Nginx на портах 80/443.

## 🌐 Обновление DNS записей

Ваш домен send-buddy.ru должен указывать на **194.67.124.90** (обновите A-записи!).

### Как обновить:

1. Зайдите в DNS панель: https://dnsadmin.hosting.reg.ru/manager/ispmgr
2. Логин: ce115049195
3. Пароль: 1771!_VnrHgCd87
4. Найдите домен send-buddy.ru
5. Обновите A-записи:
   - `@` → **194.67.124.90**
   - `www` → **194.67.124.90**

### Проверка DNS:

```bash
# На сервере или локально
dig send-buddy.ru +short
# Должно вернуть: 194.67.124.123

dig www.send-buddy.ru +short
# Должно вернуть: 194.67.124.123
```

## 🔗 Подключение к серверу

Используйте **публичный IP** для подключения:

```bash
ssh root@194.67.124.90
```

Пароль: `n3Ij63UBc4osWD6m`

## 📊 Структура сети

```
Интернет
    ↓
194.67.124.90 (Плавающий IP)
    ↓
Сервер Reg.ru
    ├── Публичный интерфейс: 194.67.124.90
    └── Приватный интерфейс: 192.168.0.107
         └── Приватная сеть (для связи с другими серверами)
```

## ✅ Что это значит для SendBuddy

1. **Подключение**: Используйте `194.67.124.90` для SSH
2. **Домен**: Обновите DNS записи на `194.67.124.90`
3. **Порты**: Откройте 22, 80, 443 в firewall
4. **SMTP**: Не нужен (используем SMS, не email)

## 🔍 Проверка подключения

```bash
# Проверка доступности сервера
ping 194.67.124.90

# Проверка SSH
ssh -v root@194.67.124.90

# Проверка портов (после настройки)
curl http://194.67.124.90:3001/api/health
```

