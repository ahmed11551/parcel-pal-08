# ✅ Ваш тариф: HP C2-M2-D40

## 📊 Характеристики

- ✅ **CPU**: 2 ядра (2.8 ГГц) - **подходит** (минимум)
- ✅ **RAM**: 2GB DDR4 - **подходит** (минимум, но будет работать)
- ✅ **Диск**: 40GB SSD NVMe - **отлично** (даже больше минимума)
- ✅ **ОС**: Ubuntu 22.04 LTS - **идеально**
- ✅ **IP**: Плавающий IP - **хорошо**
- 💰 **Стоимость**: 1 350₽/мес (2.01₽/час)

## ✅ Оценка тарифа

### Подходит для:
- ✅ Один проект SendBuddy
- ✅ Тестирование и разработка
- ✅ Небольшая и средняя нагрузка
- ✅ До 50-100 одновременных пользователей

### Ограничения:
- ⚠️ 2GB RAM - будет работать, но без большого запаса
- ⚠️ При высокой нагрузке может быть медленнее
- ⚠️ Для двух проектов будет тесно

## 🎯 Рекомендации

### Для начала - отлично!

Этот тариф **подходит для старта**. Вы сможете:
- Развернуть SendBuddy
- Протестировать работу
- Запустить в продакшен
- Обслуживать небольшую аудиторию

### Если нагрузка вырастет:

Можно будет масштабировать до:
- 4 CPU, 4GB RAM (для большей нагрузки)
- Или добавить еще один сервер для второго проекта

## 📋 Что делать после покупки

1. **Сохраните данные доступа:**
   - IP адрес сервера
   - Логин (обычно `root`)
   - Пароль (если указали)

2. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш-ip-адрес
   ```

3. **Следуйте инструкциям:**
   - [REG_RU_QUICK_START.md](REG_RU_QUICK_START.md) - быстрый старт
   - [DOCKER_COMPOSE_GUIDE.md](DOCKER_COMPOSE_GUIDE.md) - детальная инструкция

## ⚙️ Оптимизация для 2GB RAM

Чтобы максимально эффективно использовать 2GB RAM:

### 1. Настройте лимиты для контейнеров

В `docker-compose.yml` добавьте:

```yaml
services:
  postgres:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  backend:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  frontend:
    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 64M
```

### 2. Настройте swap (виртуальная память)

```bash
# Создайте swap файл 2GB
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# Сделайте постоянным
echo '/swapfile none swap sw 0 0' | tee -a /etc/fstab
```

### 3. Оптимизируйте PostgreSQL

В `docker-compose.yml` добавьте переменные окружения для PostgreSQL:

```yaml
postgres:
  environment:
    POSTGRES_DB: sendbuddy
    POSTGRES_USER: sendbuddy_user
    POSTGRES_PASSWORD: ${DB_PASSWORD}
    # Оптимизация для малой памяти
    POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
  command: >
    postgres
    -c shared_buffers=128MB
    -c effective_cache_size=512MB
    -c maintenance_work_mem=64MB
    -c checkpoint_completion_target=0.9
    -c wal_buffers=16MB
    -c default_statistics_target=100
    -c random_page_cost=1.1
    -c effective_io_concurrency=200
    -c work_mem=4MB
    -c min_wal_size=1GB
    -c max_wal_size=2GB
```

## 📊 Мониторинг ресурсов

После запуска проверяйте использование:

```bash
# Использование памяти
free -h

# Использование CPU
htop

# Использование диска
df -h

# Статус контейнеров
docker stats
```

## ⚠️ Важно знать

### При 2GB RAM:

1. **Не запускайте лишние сервисы** на сервере
2. **Мониторьте использование памяти** регулярно
3. **Настройте swap** для подстраховки
4. **Ограничьте ресурсы контейнеров** через docker-compose

### Если памяти не хватает:

Симптомы:
- Медленная работа
- Ошибки "Out of memory"
- Контейнеры перезапускаются

Решение:
- Настройте swap (см. выше)
- Ограничьте ресурсы контейнеров
- Или масштабируйте до 4GB RAM

## ✅ Итог

**Да, этот тариф подходит!**

- ✅ Минимальные требования выполнены
- ✅ Будет работать для одного проекта
- ✅ 40GB диска - отличный запас
- ✅ Можно начать работу прямо сейчас

**Следующие шаги:**
1. Купите сервер
2. Следуйте [REG_RU_QUICK_START.md](REG_RU_QUICK_START.md)
3. При необходимости оптимизируйте под 2GB RAM (см. выше)

Удачи с деплоем! 🚀

