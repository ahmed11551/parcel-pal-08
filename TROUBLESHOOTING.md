# 🔧 Решение проблем

## Проблема: Frontend не открывается (http://localhost:5173)

### Причина 1: Зависимости не установлены

**Решение:**
```bash
npm install
```

### Причина 2: .env файл отсутствует

**Решение:**
```bash
# Создайте .env файл в корне проекта
echo "VITE_API_URL=http://localhost:3001/api" > .env
```

Или запустите скрипт настройки:
```bash
./setup.sh
```

### Причина 3: Frontend не запущен

**Решение:**
```bash
npm run dev
```

Должно появиться сообщение:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### Причина 4: Порт 5173 занят

**Решение:**
1. Найдите процесс, использующий порт:
```bash
lsof -i :5173
```

2. Остановите процесс или измените порт в `vite.config.ts`:
```typescript
export default defineConfig({
  server: {
    port: 5174, // другой порт
  },
})
```

### Причина 5: Node.js не установлен или не в PATH

**Решение:**
1. Проверьте установку:
```bash
node --version
npm --version
```

2. Если не установлен, установите через:
   - [nvm](https://github.com/nvm-sh/nvm) (рекомендуется)
   - [Официальный сайт Node.js](https://nodejs.org/)

3. После установки перезапустите терминал

## Проблема: Backend не запускается

### Причина 1: База данных не создана

**Решение:**
```bash
createdb -U postgres sendbuddy
# или
psql -U postgres -c "CREATE DATABASE sendbuddy;"
```

### Причина 2: Неправильные настройки в backend/.env

**Решение:**
Проверьте файл `backend/.env`:
- Правильный пароль PostgreSQL
- Правильное имя базы данных
- Порт 3001 свободен

### Причина 3: Зависимости не установлены

**Решение:**
```bash
cd backend
npm install
```

## Проблема: Ошибки при установке зависимостей

### npm ERR! network

**Решение:**
```bash
# Очистите кеш
npm cache clean --force

# Попробуйте снова
npm install
```

### npm ERR! EACCES (права доступа)

**Решение:**
Не используйте sudo для npm install. Вместо этого:
```bash
# Исправьте права на директорию
sudo chown -R $(whoami) ~/.npm
sudo chown -R $(whoami) node_modules
```

## Проблема: CORS ошибки

**Решение:**
Убедитесь, что в `backend/.env` указан правильный `FRONTEND_URL`:
```env
FRONTEND_URL=http://localhost:5173
```

## Проблема: API не отвечает

### Проверка backend

1. Убедитесь, что backend запущен:
```bash
curl http://localhost:3001/api/docs
```

2. Проверьте логи backend в терминале

3. Проверьте Swagger: http://localhost:3001/api/docs

### Проверка frontend

1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Network tab
3. Убедитесь, что `VITE_API_URL` правильный в `.env`

## Проблема: База данных не подключается

### Проверка PostgreSQL

1. Убедитесь, что PostgreSQL запущен:
```bash
# macOS
brew services list | grep postgresql

# Linux
sudo systemctl status postgresql
```

2. Проверьте подключение:
```bash
psql -U postgres -d sendbuddy
```

3. Если не работает, запустите PostgreSQL:
```bash
# macOS
brew services start postgresql

# Linux
sudo systemctl start postgresql
```

## Быстрая диагностика

Запустите эти команды для проверки:

```bash
# 1. Проверка Node.js
node --version && npm --version

# 2. Проверка зависимостей
ls node_modules 2>/dev/null && echo "✅ Frontend зависимости установлены" || echo "❌ Нужно: npm install"
ls backend/node_modules 2>/dev/null && echo "✅ Backend зависимости установлены" || echo "❌ Нужно: cd backend && npm install"

# 3. Проверка .env файлов
[ -f .env ] && echo "✅ Frontend .env существует" || echo "❌ Нужно создать .env"
[ -f backend/.env ] && echo "✅ Backend .env существует" || echo "❌ Нужно создать backend/.env"

# 4. Проверка базы данных
psql -U postgres -lqt | cut -d \| -f 1 | grep -qw sendbuddy && echo "✅ База данных существует" || echo "❌ Нужно создать базу данных"

# 5. Проверка портов
lsof -i :5173 && echo "⚠️  Порт 5173 занят" || echo "✅ Порт 5173 свободен"
lsof -i :3001 && echo "⚠️  Порт 3001 занят" || echo "✅ Порт 3001 свободен"
```

## Полная переустановка

Если ничего не помогает:

```bash
# 1. Удалите node_modules
rm -rf node_modules backend/node_modules

# 2. Удалите package-lock.json
rm -f package-lock.json backend/package-lock.json

# 3. Пересоздайте .env файлы
./setup.sh

# 4. Установите зависимости заново
npm install
cd backend && npm install && cd ..

# 5. Запустите
cd backend && npm run start:dev &
cd .. && npm run dev
```

## Получить помощь

Если проблема не решена:
1. Проверьте логи в терминале
2. Проверьте консоль браузера (F12)
3. Проверьте [SETUP_GUIDE.md](./SETUP_GUIDE.md)
4. Создайте issue с описанием проблемы и логами

