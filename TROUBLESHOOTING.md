# 🔍 Решение проблем при деплое

## Проблема: Backend контейнер не запускается

### Проверка логов:

```bash
# Логи backend контейнера
docker compose logs backend

# Логи всех контейнеров
docker compose logs

# Статус контейнеров
docker compose ps
```

### Частые проблемы:

1. **База данных не готова**
   - Проверьте: `docker compose logs postgres`
   - Подождите пока база данных полностью запустится

2. **Переменные окружения не установлены**
   - Проверьте: `cat .env.production`
   - Убедитесь что все переменные заполнены

3. **Порт занят**
   - Проверьте: `netstat -tulpn | grep 3001`
   - Остановите процесс или измените порт

4. **Ошибка в коде**
   - Проверьте логи: `docker compose logs backend`
   - Исправьте ошибки и пересоберите

### Команды для диагностики:

```bash
# Перезапуск с логами
docker compose down
docker compose up -d
docker compose logs -f backend

# Проверка здоровья
docker compose ps
curl http://localhost:3001/api/health

# Вход в контейнер
docker compose exec backend sh
```
